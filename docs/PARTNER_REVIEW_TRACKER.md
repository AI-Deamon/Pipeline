# Partner Review Tracker

Running log from ongoing discuss-first, fix-later review sessions covering security pipeline,
system design, architecture, codebase, UI/UX, and infra. Nothing here is applied yet — this is
the shared backlog of findings/ideas until we decide together what to act on.

**Status legend:** `Open` (not decided) · `Agreed` (we decided to fix) · `Deferred` (decided not now) · `Done` (fixed)

---

## Auth / Token Lifecycle

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 1 | Refresh token JWT has no `exp` claim — valid forever server-side even though the cookie is session-only client-side | `backend/app/core/security.py` (`create_refresh_token`) | High | **Done** | Mitigated partially by `max_age=0` cookie, but no server-side expiry or revocation if token leaks (XSS, logs, synced browser). **Fix later, after discussion phase.** |
| 2 | Access and refresh tokens are interchangeable — `/refresh` just decodes and reads `sub`, no `type` claim check | `backend/app/api/auth.py:141` (`refresh_access_token`) | Medium | **Done** | A stolen access token can be replayed at `/refresh` to mint new tokens, extending exposure past the 60-min access token window |
| 3 | JWT secret falls back to an ephemeral random key with only a warning if `JWT_SECRET_KEY` unset | `backend/app/core/security.py:12-19` | Medium | **Done** | Should hard-fail outside dev/test, mirroring the `_UNRESTRICTED_RATE_LIMIT_ENVS` fail-strict pattern already used in `auth.py`. **Fix later, after discussion phase.** |
| 4 | `ACCESS_TOKEN_EXPIRE_MINUTES = 60` module constant vs. `create_access_token`'s own default of 15 min if `expires_delta` omitted | `backend/app/core/security.py:23,38` | Low | **Done** | Currently harmless (callers always pass `expires_delta`), but a latent trap for a future caller. **Fix later, after discussion phase.** |
| 13 | Logout is client-side only and cannot actually clear the session — no server-side `/logout` endpoint exists, and the frontend's `document.cookie = '...max-age=0'` trick cannot touch `httpOnly` cookies (JS has no access to them) | `src/hooks/useAuth.tsx:109-119` (frontend); missing counterpart in `backend/app/api/auth.py` | High | **Done** | Directly compounds #1 — since the refresh cookie never expires server-side either, a user who clicks "logout" on a shared machine is still fully authenticated afterward. Real fix needs a server `/logout` route that clears cookies via `Response.delete_cookie` (or expires them) and, ideally, a revocation check (ties to the `token_version` idea under #1) |

## Rate Limiting / Concurrency

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 5 | Rescan rate limiter is in-memory, single-process only — silently becomes `3 × worker_count`/hour if uvicorn/celery ever scale to multiple workers | `backend/app/services/rescan_rate_limit.py` | Medium | **Done** | Rewrote `check()`/`reset()` to use a Redis sorted-set sliding window (shared `app.services.cache._get_client()` connection — Redis is already a live dependency for Celery/caching) with an atomic Lua script (`ZREMRANGEBYSCORE` + `ZCARD` + `ZADD`/`EXPIRE` in one round trip) so concurrent processes can't race past the limit. Falls back to the original in-memory dict implementation (kept as `_check_in_memory`) when Redis isn't reachable, matching `cache.py`'s existing degrade-gracefully pattern — so this still works in the test env with no Redis running. Regression test: `tests/test_rescan_rate_limit.py` — `TestInMemoryFallback` proves the fallback path still enforces the limit; `TestRedisBackedSharedAcrossProcesses` (skipped when Redis isn't reachable, ran manually against a throwaway local `redis-server` instance and passed) proves the limit is now shared across separate client instances simulating two worker processes, closing the `3 × worker_count` bug directly. Full suite: 459 passed. |

## Infra / SonarQube (from earlier resource discussion)

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 6 | No memory/CPU limits on any compose service | `docker/docker-compose.yml` | Medium | **Agreed** | SonarQube (JVM+ES) could burst and starve backend/celery under load, though not observed yet at current usage |
| 7 | SonarQube JVM heap uncapped (Web/CE/Search all default) | `docker/docker-compose.yml` (`sonarqube` service) | Low | **Agreed** | Discussed heap-cap risks in detail: undersizing CE heap can fail large scans, undersizing Search heap can crash ES → whole Sonar UI down. Want to check real scan sizes/logs before picking numbers |
| 8 | SonarQube always-on vs. on-demand (Compose `profiles`) | `docker/docker-compose.yml`, `Agent/Jenkinsfile:215` | Idea | **Agreed** | Would free ~2.2GB idle RAM; costs ~60-120s cold start per scan and loses always-on dashboard access. Not needed yet — current usage is ~19% of 15.5GB |
| 9 | No `restart: unless-stopped` on `backend`, `frontend`, `sonarqube` (only postgres/redis/celery_worker have it) | `docker/docker-compose.yml` | Low | **Agreed** | If these crash (e.g. OOM), they stay down until manually restarted |
| 10 | No log rotation configured for any container (`json-file` default, no `max-size`) | `docker/docker-compose.yml` | Low | **Agreed** | Slow unbounded disk growth on long-lived containers |
| 11 | Uvicorn runs single worker process, no `--workers` flag | `docker/backend.Dockerfile` | Low | **Agreed** | 8 idle cores unused for concurrent API load; note dependency on #5 if changed |
| 12 | `SONARQUBE_TOKEN` and DB creds present in `.env.dev` | `.env.dev` | Info | Checked | Confirmed `.env*` is gitignored (`.gitignore:19-21`) — not committed, no action needed unless shared insecurely elsewhere |

## Real-time / WebSocket

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 14 | Both WebSocket endpoints (`/ws/scans`, `/ws/dashboard`) have zero authentication — no token/cookie check before `manager.connect()` | `backend/app/websockets/__init__.py:13-59` | High | **Done** | Anyone who can reach the backend can subscribe to live scan updates (stage results, severity summaries, error messages) with no login — for a security-scanning tool this leaks vulnerability findings in real time. Every REST route goes through `get_current_user`; these don't. Needs a token check (e.g. pass JWT as query param or subprotocol, since browsers can't set custom headers on WS handshake) before `accept()` |

## Async Infra / Scan Recovery

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 15 | Jenkins admin token can be exfiltrated to an attacker-controlled server via a project's `target_ip` field | `backend/app/api/scans/utils.py` (`resolve_jenkins_base_url`), `backend/app/services/reporting/fetcher.py:52-63`, fires from both `report_tasks.py` and `scan_recovery.py` | **Critical** | **Done** | `target_ip` is user-supplied, validated only as "looks like an IPv4 address" — no private/loopback/metadata-IP blocklist, no ownership check. It replaces the entire Jenkins base URL used to fetch reports, and `fetch_artifact()` attaches `Authorization: Basic base64("admin:"+JENKINS_TOKEN)` to that request. Concrete attack: set `target_ip` to an attacker's IP → real Jenkins admin credential gets sent to them. Also SSRF-capable against internal targets (e.g. cloud metadata `169.254.169.254`) | Fix: `resolve_jenkins_base_url` no longer branches on `target_ip` at all — always returns `settings.JENKINS_BASE_URL` (confirmed via `docs/jenkins_pipeline_architecture.md` that `target_ip` is the scanned application's IP, not a Jenkins endpoint; the override was a semantic bug, not a multi-Jenkins feature). New `tests/test_jenkins_base_url_resolution.py` covers it. |
| 16 | `cleanup_expired_reports` task is never scheduled — dead code, unbounded DB growth | `backend/app/tasks/cleanup_tasks.py:11-17` (docstring claims "runs daily at 3 AM"); no `beat_schedule` exists in `celery_app.py`, no `celery beat` process launched anywhere | High | **Done** | `expires_at` is set on every `ScanReportDB` row (90-day default) implying intended pruning, but nothing calls the task — `ScanReportDB` (stores full raw report JSON) grows forever | Added `celery_app.conf.beat_schedule` (daily 3 AM crontab) and `-B` to the celery_worker container command to actually run the embedded beat scheduler. New tests confirm the schedule is registered. |
| 17 | Hard Celery task-time-limit (`SIGKILL`) can silently lose in-flight report processing with no redelivery | `backend/app/core/celery_app.py` (`task_time_limit=600`), no `acks_late`/`task_reject_on_worker_lost` set | Medium | **Done** | Default Celery acks the task *before* execution, so a `SIGKILL` on timeout bypasses the task's own `except: retry` entirely — task is gone, not requeued. A slow ZAP artifact fetch (30s timeout × multiple artifacts) could plausibly exceed 600s and silently drop the scan's report processing with zero error logged | Added `task_acks_late=True` + `task_reject_on_worker_lost=True` to celery_app.py — a killed/crashed worker now gets its task redelivered instead of silently losing it (existing report-upsert idempotency already makes redelivery safe). |
| 18 | `poll_jenkins_for_active_scans` can stall recovery on any non-404 Jenkins error (500, timeout, TLS hiccup) | `backend/app/services/scan_recovery.py:91-101,124-134` | Medium | **Done** | Only 404 is treated as "build gone"; any other error just logs and returns, leaving the scan `QUEUED`/`RUNNING` until the separate timeout-based `recover_stuck_scans` sweep eventually catches it — soft finding since that backstop exists, but no distinction between "genuinely still running" and "Jenkins is flaky" | Added a Prometheus counter (`JENKINS_POLL_ERRORS_TOTAL`, labeled by check_type) incremented on every non-404 Jenkins polling error, giving operators real visibility distinguishing "Jenkins is flaky" from silence. The timeout-based recover_stuck_scans backstop was already correct and untouched. |
| 19 | `recover_stuck_scans` never cancels the underlying Jenkins build when timing out a scan | `backend/app/services/scan_recovery.py:196-262` | Low | **Done** | DB marks the scan `FAILED`, but Jenkins is never told to abort — the build keeps consuming an executor slot. Resource-hygiene gap, not a correctness bug (late callback is correctly no-op'd via terminal-state check) | Added `JenkinsClient.stop_build()` and wired a best-effort call into `recover_stuck_scans` when timing out a scan with a known build number — genuinely-hung Jenkins builds no longer keep consuming an executor slot after Sentinel gives up on them. 3 new tests cover the abort call, graceful failure handling, and the no-build-number skip case. |
| 20 | Jenkins-vs-generic HTTP request detection done via substring match on URL (`"jenkins" in self.base_url.lower()`) | `backend/app/infrastructure/http/client.py:106` | Low | **Done** | `HttpClient.__init__` now takes an explicit `is_jenkins: bool = False` constructor flag instead of re-deriving it from the URL string on every request; `JenkinsClient` passes `is_jenkins=True` at construction. The caller always knows which service it's talking to — no need to guess from a substring that could be absent from a real Jenkins hostname. Regression tests added to `tests/test_http_client_jenkins_response.py` (`TestJenkinsDetectionIsExplicitNotUrlBased`): a client explicitly flagged `is_jenkins=True` against a URL with no "jenkins" in it still gets crumb-fetch + raw-response treatment; a client left at the default `is_jenkins=False` against a URL that *does* contain "jenkins" no longer gets that treatment — directly proving detection no longer depends on the URL text. Updated the existing `test_post_request_to_jenkins_hostname_still_returns_raw_response` test to pass `is_jenkins=True` explicitly, since it now needs to (previously relied on the URL substring). Full suite: 482 passed. |

## System Design / Architecture

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 21 | Project-group RBAC scoping is dead code — group-scoped grants silently give zero access | `backend/app/services/rbac_service.py:44-70` (`get_effective_project_ids`) | High | **Done** | For a `project_group`-scoped assignment, the method adds the **group_id** itself to the effective `project_ids` set instead of expanding it to member project IDs. Since access checks compare against real `project_id`s, anyone granted access via a group gets **no accessible projects at all**. Fails closed (not a leak) but defeats the entire purpose of `ProjectGroupDB` grants used in `projects.py`/`issues.py` |
| 22 | No DB-level foreign keys from `ScanDB`/`ScanReportDB`/`IssueDB`/`ScanAssignmentDB`.`project_id` to `projects.project_id` | `backend/app/models/db_models.py:31,76,114,214` | Medium | **Done** | All plain indexed `String` columns, not real FKs (unlike `RescanRequestDB`, which does use FKs with CASCADE). Nothing stops a project delete or a bad `project_id` from a Jenkins callback from orphaning scans/issues/reports — an invariant RBAC scoping, `project_grouping.py`, and portfolio rollups all silently depend on | Added FK constraints (scans, scan_reports, issues, scan_assignments -> projects) via NOT VALID (Postgres-only; SQLite has no ALTER TABLE ADD CONSTRAINT, so this cleanly no-ops in the test suite). Verified directly against the live dev Postgres (docker-postgres-1): found real orphaned data first (39 scan_reports, 72 issues across 4 deleted project_ids — confirms this exact bug already happened), confirmed NOT VALID applied successfully despite the orphans, confirmed a new insert with a bad project_id is now rejected, confirmed re-running is idempotent. Did not touch the existing orphaned rows — that's a separate cleanup decision, not this fix's scope. |
| 23 | `docs/SYSTEM_DESIGN.md` and `docs/FINALIZED_ARCHITECTURE.md` describe an older, materially different system | `docs/SYSTEM_DESIGN.md`, `docs/FINALIZED_ARCHITECTURE.md` | Medium (doc drift) | **Done** | Didn't rewrite either doc wholesale — a full rewrite risks introducing new inaccuracies without exhaustively re-verifying the entire current frontend/backend against it, and `SYSTEM_DESIGN.md` in particular describes a fundamentally different (mock/localStorage) system that isn't worth resurrecting. Instead: added a clear "ARCHIVED — do not use" banner to the top of `SYSTEM_DESIGN.md` explaining what it actually describes and pointing to `AGENTS.md`. `FINALIZED_ARCHITECTURE.md` is different — what it covers (base API contract, casing, project/scan CRUD, state mapping) is still accurate, just incomplete — so instead of archiving it, added a note explaining what's missing (auth, RBAC, Issues, project groups, WebSockets) and fixed one real accuracy gap found while checking it: the scan-state mapping list was missing `SKIPPED`, verified against the actual `ScanState` enum. |
| 24 | `docs/architecture-overview.md`'s file map for the scans module is wrong/stale | `docs/architecture-overview.md:29-31,275-281` | Low (doc drift) | **Done** | Fixed the API-layer diagram box and the backend file table: replaced the nonexistent `triggers.py`/`callbacks.py`/`management.py` names with the real `routes.py`/`callback.py`/`state.py`/`utils.py`, and added the previously-omitted `issues.py`, `portfolio.py`, `project_groups.py`, `reports.py`, `users.py`, `rbac_service.py`, and `issue_tasks.py` rows. Verified every path/filename added against `ls` of the actual `backend/app/api/`, `backend/app/api/scans/`, and `backend/app/tasks/` directories before writing them in. |
| 25 | Docs claim staging runs 4 uvicorn workers; it doesn't — and going to multi-worker would silently break two in-process subsystems | `docs/architecture-overview.md:154` vs `docker/backend.Dockerfile:20` (no `--workers` flag, defaults to 1) | Low (doc drift, but masks a real gap) | **Done** | Corrected the Environment Comparison table's Workers row to state the real behavior (1, no `--workers` flag) instead of the fabricated "4 workers." Added a callout note directly under the table explaining *why* this matters beyond doc accuracy: scaling to multiple uvicorn workers would silently break `main.py`'s in-process `threading.Thread` scan-recovery loop (each worker would run its own independent, racing copy) and `websockets/manager.py`'s in-process connection dict (no cross-process broadcast fan-out), so a future "just add `--workers 4`" perf change should be treated as a real migration rather than a flag flip. |
| 26 | Single Jenkins instance is an unaddressed SPOF | `backend/app/core/config.py:10` (`JENKINS_BASE_URL: str`, single required string, no pool/failover concept) | Medium | **Agreed** | If Jenkins goes down mid-scan, recovery itself depends on polling the same Jenkins instance to decide scan outcomes — no distinct "Jenkins unreachable" handling path documented or visible in code; in-flight scans just sit until the recovery timeout elapses |
| 27 | `ScanReportDB.raw_report` stores full unbounded tool output as a plain string column, no size guard | `backend/app/models/db_models.py:86`, populated in `backend/app/services/reporting/fetcher.py:97,138,201` | Low/Medium | **Done** | Added `_cap_raw_report()` in `fetcher.py` — caps stored `raw_report` at 5MB (UTF-8 bytes), appending a truncation notice when a report is cut. Applied at both write sites (`fetch_and_process_tool`'s `raw_json`, `create_sonar_link`'s `combined_raw`); `raw_report=None` still passes through unchanged. Parsed `findings`/`severity_summary` are computed from the full untruncated payload before the cap is applied, so app functionality (issue creation, risk scoring) is unaffected — only the raw-download convenience endpoint sees the capped value. Didn't do compression/blob storage since that would require a column type migration on a live table; truncation directly closes "no size guard" without touching the schema. Regression test: `tests/test_raw_report_size_cap.py` (small report unchanged, `None` unchanged, oversized report truncated to ≤5MB with notice suffix). Full suite: 458 passed. |

## Positive findings (things done well — worth naming, not just gaps)

| # | What | Location | Why it's good |
|---|------|----------|----------------|
| P1 | Callback endpoint uses `hmac.compare_digest` for token comparison + row-locking + idempotency digest | `backend/app/api/scans/utils.py` (`_validate_callback_auth`), `callback.py` | Timing-safe comparison, handles concurrent Jenkins retries correctly |
| P2 | Jenkins-supplied `artifact_url` is validated to only allow `http(s)://` or absolute path before it's ever stored, and is rendered with `rel="noopener noreferrer"` on the frontend | `backend/app/api/scans/utils.py` (`_validate_artifact_url`), `src/pages/ScanStatusPage.tsx:104-113` | Closes the `javascript:` URI XSS vector right at the trust boundary where untrusted (Jenkins) data enters the system |
| P3 | Login timing-attack defense — dummy password hash verified even for nonexistent usernames | `backend/app/api/auth.py` (`login_for_access_token`) | Prevents username enumeration via response-time side channel |
| P4 | `fetcher.py` builds its own Jenkins artifact URL from `jenkins_base_url` + fixed job path, and never trusts the callback-supplied `artifact_url` for actually fetching data | `backend/app/services/reporting/fetcher.py` | The validated-but-cosmetic `artifact_url` field can't be used for SSRF against the fetch path, since fetching uses a server-constructed URL, not client input |
| P5 | Every report-store path deletes any existing `(scan_id, tool_name)` row before inserting, making Celery-level retries idempotent | `backend/app/services/reporting/fetcher.py:104-118,145-162,208-232` | Automatic task retries (from finding #17) cannot create duplicate report rows — consistently applied across trivy/zap/depcheck/nmap/sonar |
| P6 | `scan_recovery.py` uses a correct two-phase lock pattern: cheap unlocked prefetch of candidate IDs, then `with_for_update()` + re-verify state immediately before mutating, committing per-row | `backend/app/services/scan_recovery.py:150-190,213-248` | Mirrors the `with_for_update()` pattern already confirmed in `callback.py` — correctly guards against a recovery sweep racing a live Jenkins callback for the same scan |
| P7 | Celery configured with `task_serializer="json"`/`accept_content=["json"]`, not pickle | `backend/app/core/celery_app.py:17-19` | Explicitly closes off the classic pickle-deserialization RCE vector, even if Redis were ever exposed or writable |
| P8 | Issue-identity model cleanly separates current state, audit trail, and per-scan presence | `backend/app/models/db_models.py:109-184` (`IssueDB` + `IssueHistoryDB` + `IssueScanDB`), real unique constraint `ix_issues_issue_id_project` | Exactly the right shape for tracking findings across repeated scans (new/resurfaced/resolved) — a coherent core abstraction, easy to overlook next to finding #21 |
| P9 | RBAC fails closed | `backend/app/services/rbac_service.py:76-83` (`has_project_access`) | Returns `False` when effective project IDs is empty for non-admins — so even with the group-scoping bug (#21), the failure mode is "user sees nothing," the safe direction for a multi-tenant security tool to break in |
| P10 | "One active scan per project" invariant enforced at the DB layer, not just app code | `backend/app/models/db_models.py:56-58` (`ix_scans_project_state`) | Real architectural safeguard against two concurrent Jenkins builds writing conflicting callback state into the same project; consistently referenced across docs and code |
| P11 | `/metrics` endpoint uses HTTP Basic with `secrets.compare_digest` against a dedicated `METRICS_TOKEN`, kept separate from the JWT/API-key auth used elsewhere | `backend/app/main.py:191-216` | Sensible separation between operational scraping and user-facing auth |

## Security Pipeline — Open Questions (not yet investigated)

| # | Question | Status |
|---|----------|--------|
| Q1 | How does `shouldRun()` in the Jenkinsfile decide which of the 10 stages run per scan job? | Open |
| Q2 | What exactly does `_validate_callback_auth` check, and how is the callback token provisioned/rotated? | Open |
| Q3 | How is `resolve_jenkins_base_url` / multi-Jenkins-instance support structured, if at all? | Open |

## UI / UX

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 56 | "Reject" button on rescan approvals doesn't reject anything — it triggers a brand-new verification scan | `src/pages/PendingVerificationPage.tsx:36-42,175-179`, `src/services/api.ts:393-398` | High | **Done** | Reject mutation is wired to `triggerVerifyScan(projectId, tool)` — the same endpoint "Approve" effectively drives. There is no `rejectRescan` call anywhere in `api.ts`. A reviewer who clicks "Reject" expecting a fix to be marked rejected/closed instead silently re-triggers a scan, with no indication anything different happened |
| 57 | Rescan-approvals "Live/Offline" indicator is permanently hardcoded to "Live" | `src/pages/PendingVerificationPage.tsx:15` (`const [wsConnected] = useState(true)` — setter never called) | Medium | **Done** | `useRescanWebSocket` now tracks real connection state (`useState` set on `ws.onopen`/`onclose`/unmount) and returns `{ connected }`; `PendingVerificationPage` uses that instead of the hardcoded local state. Regression test: `src/tests/hooks/useRescanWebSocket.test.tsx` (mocked `WebSocket`) — asserts `connected` starts `false`, flips `true` on `onopen`, and flips back `false` on `onclose`. Full frontend suite: 133 passed. |
| 58 | Severity is never shown as text on issue cards, and the icon only distinguishes 2 of 4 levels | `src/components/IssueCard.tsx:17-25,46` | High (accessibility) | **Done** | `severityIcon()` gives critical/high a red triangle and medium/low/info the identical orange circle — no text label anywhere on the card. This is the card used throughout the primary triage workflow (`IssuesTriagePage.tsx`); a colorblind or hurried triager cannot distinguish medium from low, and critical/high render identically too. Misjudging severity has real consequences on a security triage tool |
| 59 | Severity color for "Low" is inconsistent between the shared chart palette and the rescan-approval card | `src/components/RescanRequestCard.tsx:13-21` (local `severityClass()`, `low: 'bg-blue-100 text-blue-700'`) vs `src/utils/severity.ts` (`low: '#16a34a'`, green — used by the pie/trend charts) | Medium | **Done** | While fixing this, found a second, identical duplicate the finding didn't name: `src/pages/ToolDetailViewPage.tsx` had its own copy of the exact same function with the exact same blue-for-low mismatch. Added `severityPillClass()` to `severity.ts` (green for low, matching `SEVERITY_HEX`) and switched both `RescanRequestCard.tsx` and `ToolDetailViewPage.tsx` to import it instead of keeping local copies — closing the drift at its source rather than patching one instance. Regression test: `src/tests/utils/severity.test.ts` — asserts `severityPillClass('low')` is green (not blue) and matches `severityHex('low')`'s color family, plus case-insensitivity and unknown-severity fallback. Full frontend suite: 133 passed. |
| 60 | Fetch errors are indistinguishable from "zero data" on Dashboard and Issues Triage | `src/pages/DashboardPage.tsx:60-77` (only destructures `data`/`isLoading`, never `error`), `src/pages/IssuesTriagePage.tsx:50-66,104-107,307-322` | Medium/High | **Done** | A failed `api.projects.list` call renders the exact same "No projects yet" empty state as a genuinely empty account. Same for issues — a failed tool-issue fetch renders "No issues match your filters" instead of an error. An admin could see "no issues," assume the project is clean, when the data simply never loaded |
| 61 | No confirmation before Reject/Approve, and the approve mutation only invalidates its own query — other views go stale | `src/pages/PendingVerificationPage.tsx:32-41,166-179` | Medium | **Done** | The invalidation half was already fixed in an earlier round (both mutations invalidate `pending-verification`/`tool-issues`/`my-issues`/`project-overview`). This pass added the missing confirmation step: clicking Verify Now/Reject now opens the shared `ConfirmModal` (same component used elsewhere in the app) showing the issue title and a clear description of the consequence, with the actual mutation only firing on explicit confirm; Cancel or the modal's close button discards the pending action with no API call. Regression test: `src/tests/pages/PendingVerificationPage.test.tsx` — clicking Verify Now/Reject shows the modal without calling the API; confirming calls it with the expected args; cancelling a reject leaves the reject API uncalled. Full frontend suite: 133 passed. |
| 62 | RegisterPage's show/hide-password toggle is missing the `aria-label` that LoginPage's identical control has | `src/pages/RegisterPage.tsx:84-90` vs `src/pages/LoginPage.tsx:122-129` | Low | **Done** | Added the same `aria-label={showPassword ? 'Hide password' : 'Show password'}` LoginPage already has. Regression test: `src/tests/pages/RegisterPage.test.tsx` — asserts the button is queryable by its accessible name via `getByRole('button', { name: /show password/i })`, which would fail without the label. Full frontend suite: 133 passed. |
| 63 | Header "Server Time" label is misleading — it's the browser's local clock, not server time | `src/components/Layout.tsx:137,177-180,295-300` | Low | **Done** | Relabeled to "Local Clock (IST)" — honestly describes what it is (the viewer's own browser clock) without claiming server authority, while leaving the underlying IST-hardcoded display behavior unchanged since I don't know whether that's a deliberate business requirement (e.g. an India-based ops team) or an oversight; that's a product call, not something to guess at silently. No test needed for a static label string; verified visually via the source. |

## Test Coverage

**Coverage check against the 7 known bugs already found — none have a test that would catch them:**

| Bug | Test file checked | Result |
|---|---|---|
| #35/#36 IDOR (issue detail, scans/routes.py) | `tests/test_issues_rbac.py`, `tests/test_projects_rbac.py` | Scoped non-admin fixture users (`lead-1`→proj-a, `dev-1`→proj-b) are created but **never used** — every test in these files calls the API as admin only. No dedicated RBAC test file exists for `scans/routes.py` at all |
| #21 RBAC group-scoping dead code | `tests/test_rbac_service.py::TestRbacServiceProjectAccess` | Only tests direct `scope_type="project"` assignments; never exercises `scope_type="group"` or the group-expansion path in either direction |
| #46 `assign()` state-machine bypass | `tests/test_issue_assignment.py` | Tests `assign()` in isolation and `transition_status()`'s validation separately, but never calls `assign()` on an already-`verified`/`fixed` issue — the exact gap that would catch this |
| #40 Trivy cross-target dedup | `tests/test_parse_status.py` | Only test touching `parse_trivy_report` checks the corrupt-JSON error path; no realistic multi-`Results[]` fixture exists anywhere |
| #42 ZAP field-name bug | `tests/test_parse_status.py`, `tests/test_scan_report_dedup.py` | Same — only corrupt-JSON path tested; the dedup test constructs `SecurityFinding` objects directly, bypassing the parser entirely, so it structurally can't catch a parser bug |
| #1 Refresh token never expires | `tests/test_auth_cookie.py::TestAuthCookieFlow` | Verifies cookie attributes (httponly/samesite/path) and that `/auth/refresh` accepts a valid cookie, but never decodes the JWT or asserts an `exp` claim exists |
| #14 WebSocket auth | — | Zero test files reference `websocket`/`ws/scans`/`ws/dashboard` in any capacity |

**Additional findings:**

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 64 | CI never actually runs the test suite — all tests are advisory, not a merge gate | `Agent/Jenkinsfile` — zero matches for `pytest`/`npm test`/`vitest`/`jest` across all 10 stages | High | **Done** | Added `.github/workflows/tests.yml` — `Agent/Jenkinsfile` is the security-*scanning* pipeline (checks out and scans target projects, not Sentinel itself), so it was never the right place for this; GitHub Actions is. Runs `pytest tests/` and `npx tsc -b && npx vitest run` on every push/PR to `main`. All steps verified green locally before committing to the workflow (404 backend tests, 118 frontend tests, clean typecheck). Lint intentionally left out — see #92. **Note**: branch protection still needs to be turned on in GitHub repo settings to actually block merges on failure; a workflow file alone doesn't enforce that |
| 65 | `scan_orchestrator.py` and `rescan_service.py` have zero direct unit-test coverage | `backend/app/services/scan_orchestrator.py`, `backend/app/services/rescan_service.py` | High | **Done (revised)** | `rescan_service.py` now has real coverage via `tests/test_rescan_reject.py`. `scan_orchestrator.py` turned out to be genuinely dead code, not under-tested — see #93. No tests needed for a module nothing calls; effort correctly belongs on the real scan-creation path instead (`scans/routes.py::trigger_scan`), which is already covered by `test_scans_rbac.py`/`test_scan_security.py`. |
| 66 | `cache.py` and `fix_note_sanitizer.py` have zero test references anywhere | `backend/app/services/cache.py`, `backend/app/services/fix_note_sanitizer.py` | Medium | **Done** | Added `tests/test_fix_note_sanitizer.py` (11 tests) covering every redaction pattern (AWS keys, GitHub PAT, JWT, private-key headers, password/token key-value pairs, bearer tokens), plus behavioral guarantees: plain text passes through unchanged, empty string handled, the raw unredacted text is still preserved for the admin-audit path, multiple secrets in one note all get redacted, and the mask never leaks the actual secret value. Added `tests/test_cache.py` covering both branches: the no-Redis graceful-fallback path (every function must no-op safely, never raise — this is what actually runs in the default test env) and, run against a throwaway local `redis-server` on a scratch port (matching the pattern already used for `tests/test_rescan_rate_limit.py`), the real Redis-backed path — set/get/delete/delete_pattern round-tripping, and `invalidate_after_commit`'s core correctness property (finding #96): the queued invalidation must not fire until the transaction actually commits, and must be discarded entirely on rollback. Full suite: 504 passed (4 cache tests run for real without Redis reachable; the remaining 6 correctly skip and were separately verified passing against real Redis). |
| 67 | `cleanup_tasks.py` has zero test coverage | `backend/app/tasks/cleanup_tasks.py` | Medium | **Done** | Added `tests/test_cleanup_tasks.py`, covering both tasks in the module: `cleanup_expired_reports` (4 tests, written alongside the #118 fix) and `set_report_expiration` (2 tests, which caught the real `days=None` bug fixed as part of #118's entry). Zero coverage is no longer accurate for this file. |
| 68 | Systemic pattern: RBAC test fixtures create scoped non-admin users, but the negative-path assertion is never written | `tests/test_issues_rbac.py`, `tests/test_projects_rbac.py` | High | **Partially Done** | Added real cross-project denial tests to `test_issues_rbac.py`, `test_projects_rbac.py`, and a new `test_scans_rbac.py` (14 tests) as part of fixing #35/#36/#72 — these specifically exercise "user from project A denied access to project B" using a real JWT (working around `TEST_BYPASS_AUTH` short-circuiting `get_current_user`). The *pattern* fix (making this the expected norm for all future endpoints) is a team-culture/review-checklist change, not something a single pass can complete |

## Positive findings — UI/UX & Test Coverage

| # | What | Location | Why it's good |
|---|------|----------|----------------|
| P31 | `SeverityPieChart`/`TrendLineChart` both explicitly handle zero/empty data with a friendly message instead of a broken chart, and both pull colors from the same shared constant | `src/components/SeverityPieChart.tsx:50-53`, `src/components/TrendLineChart.tsx:18-27`, `src/utils/severity.ts` | Exactly the right pattern — makes finding #59's drift stand out as a real miss rather than the norm |
| P32 | `src/utils/severity.ts` is a documented single source of truth with a comment explaining the prior color-drift bug it was meant to fix | `src/utils/severity.ts` | Good intent, correctly followed by the chart components — just not by `RescanRequestCard` (#59) |
| P33 | Dashboard empty state is differentiated by role and situation instead of one generic message | `src/pages/DashboardPage.tsx:319-342` | Admin gets "Add Project" CTA, non-admin gets "contact an admin," search-with-no-results gets its own message |
| P34 | Pending Verification page correctly surfaces query errors with a retry action | `src/pages/PendingVerificationPage.tsx:139-145` | Unlike Dashboard/IssuesTriagePage (#60), this page does destructure and handle `error` properly |
| P35 | `IssueCard` has real keyboard support and semantics | `src/components/IssueCard.tsx:35-40` | Enter/Space triggers click, renders as a native `<button>` with a visible focus ring rather than a `div` with an onClick handler |
| P36 | Tight, well-isolated unit tests for the RBAC permission boundary itself | `tests/test_rbac_service.py::TestRbacServiceProjectAccess` (`test_admin_has_all_project_access`, `test_non_admin_with_assignment`, `test_non_admin_without_assignments`) | Clean Arrange/Act/Assert, tests both positive and negative case for `has_project_access` — the missing piece is group-scoping (#21), not the pattern itself |
| P37 | `transition_status()` has a genuinely solid negative test | `tests/test_issue_assignment.py::test_invalid_transition_raises` | Directly asserts `pytest.raises(ValueError, match="Invalid transition")` — exactly the kind of assertion missing for `assign()` (finding #46) |
| P38 | Callback-token timing-safety is tested by behavior, not just presence | `tests/test_callback_token_security.py`, `test_scan_security.py::TestCallbackTokenTimingSafety::test_hmac_compare_digest_behavior` | Correctly tests that `hmac.compare_digest` actually behaves in a timing-safe way, rather than just grepping source for its presence |
| P39 | Per-test schema drop/recreate gives a low-flakiness isolation strategy for the SQLite-backed integration suite | `tests/conftest.py:56-67` | Avoids cross-test state leakage — a common source of flaky RBAC/ordering bugs in a suite this size |

## System Design / Architecture

_(nothing logged yet — pending discussion)_

## Codebase — Backend API & Frontend

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 35 | IDOR — single-issue read/comment/history endpoints skip project-access checks entirely | `backend/app/api/issues.py:212-233` (`GET /issues/{issue_id}`), `:236-258` (comments), `:261-273` (history) | High | **Done** | Unlike `get_project_overview`/`get_tool_issues` in the same file, these never call `rbac.has_project_access()`. Any authenticated user (e.g. `developer` scoped only to Project A) can `GET /api/v1/issues/1337` and read full details — plus post comments — on an issue belonging to a project they have zero assignment to, just by incrementing the integer PK. The 60s response cache doesn't vary by caller either, so a cached leak serves to anyone |
| 36 | IDOR — nearly every scan-detail endpoint in `scans/routes.py` has no project scoping | `backend/app/api/scans/routes.py`: `list_scans` (76-106, doesn't even take `current_user`), `get_scan` (303-317), `get_scan_results` (320-337), `get_scan_overview` (377-391), `retry_scan_reports` (340-374), `get_project_scan_history` (394-439) | High | **Done** | Contrast with `trigger_verify_scan` in the same file, which correctly checks `rbac.can_approve_rescan`. Any authenticated user can enumerate **every scan in the system** across all projects via `GET /api/v1/scans`, read another project's raw `stage_results` via `get_scan_results`, or even re-trigger report processing for a project they don't own via `retry_scan_reports` |
| 37 | Mass-project injection on issue creation | `backend/app/api/issues.py:156-171` (`POST /issues`), `backend/app/schemas/issue.py:9` | Medium | **Done** | `project_id` comes straight from the request body with no check the caller has access to that project — a developer scoped to Project A can inject fabricated issues into another team's tracker. `status`/`assignee_id` are correctly excluded from the schema, so it's contained, not full mass-assignment. **Same root cause as #73** — one fix (add a permission check to `create_issue`) resolves both; don't treat as two separate tasks |
| 38 | `create_issue` catches bare `Exception`, always returns 400 regardless of real cause | `backend/app/api/issues.py:164-171` | Info | **Done** | Kept the 400/generic-message response to the client (didn't want to leak internals or change the API contract), but added `logger.error(..., exc_info=True)` in the except block so the real exception and traceback are now captured server-side — closes the "makes debugging/monitoring harder" complaint directly. Regression test: `tests/test_create_issue_error_logging.py` — mocks `service.create_issue` to raise, asserts the endpoint still returns 400 but the traceback is now present in the logs (previously would have been silently discarded). Full suite: 460 passed. |
| 39 | Unsanitized `href` on tool-supplied reference URLs | `src/components/FindingDetailModal.tsx:212-222` (`finding.references`) | Low | **Done** | Added `src/utils/url.ts` (`isSafeHttpUrl`) — parses the value with `new URL()` and only allows `http:`/`https:` schemes. `FindingDetailModal`'s references list now renders a real `<a href>` only for URLs that pass the check; anything else (e.g. a `javascript:` string) renders as inert plain text instead, so it's still visible to the analyst but can't execute on click. Regression test: `src/tests/components/FindingDetailModal.test.tsx` — a finding with a `javascript:alert(...)` reference alongside a legitimate `https://cwe.mitre.org/...` reference asserts no link element exists for the malicious one (rendered as plain text instead) while the safe one still renders as a proper link. Full suite: `tsc -b` clean, 123 vitest passed, lint clean. |

## File System / Repo Layout

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 28 | Live-looking SonarQube token committed in plaintext across three tracked files | `AGENTS.md:98`, `specs/006-icm-workspace-config/checklists/data-loss-checklist.md:91`, `specs/003-upgrade-sonarqube-image/quickstart.md:78` (`squ_38aedbbc9186c8bf59f9e2f70d4cd2b83ca79969`) | High | **Done** | `AGENTS.md` explicitly annotates it "verified working on 26.5" — this file is read as ambient context by any AI agent working in the repo, so the token gets echoed into future work continually. Should be rotated and scrubbed, replaced with a placeholder + reference to `docs/SECRETS_POLICY.md`, regardless of whether this specific instance is still reachable | Scrubbed the live token from all 3 files (AGENTS.md, both specs docs), replaced with env-var references + pointer to docs/SECRETS_POLICY.md. Rotating the actual token is still on you. |
| 29 | `docs/reports-implementation.md` is stale and factually contradicts current code | `docs/reports-implementation.md` vs `docs/reports-implementation-refined.md` | Medium | **Done** | The refined doc is an explicit correction pass ("What the original plan got WRONG" — e.g. claims `ScanReportDB` "is not defined" when it already exists). No deprecation banner on the original, no cross-link — real risk of loading the wrong one and acting on stale claims | Added a superseded banner pointing to reports-implementation-refined.md; kept for history rather than deleted. |
| 30 | `docs/context.md` (lowercase) is a stale AI-chat dump describing a wrong repo path, shadowing the real `docs/CONTEXT.md` | `docs/context.md` (describes tree rooted at `/home/kali_linux/Pipeline/Agent/backend/...`, doesn't match actual layout) vs `docs/CONTEXT.md` (accurate, properly maintained) | Medium | **Done** | On case-insensitive filesystems (macOS/Windows, some git tooling) `context.md`/`CONTEXT.md` collide — a real correctness risk beyond just confusion. Recommend deleting `docs/context.md` outright | Deleted outright — wrong repo path, actively misleading, and the case-collision risk with CONTEXT.md was real. |
| 31 | `.env.dev`/`.env.staging`/`.env.test` were committed to git history early in the project (now removed, gitignored) | git history only, e.g. commit `1ad613a` — not in current `git ls-files` | Low/Info | **Done** | Contained placeholder dev creds (`POSTGRES_PASSWORD=devsecops`, `JENKINS_TOKEN=dev-token`), not high-value secrets. Permanently recoverable from history unless rewritten — worth confirming these dev-only values were never reused in a real staging/prod deployment | Confirmed .env* is gitignored going forward; git history values are dev-only placeholder creds (devsecops/dev-token) — no further action without evidence of reuse in a real deployment. |
| 32 | `docs/plans/` has a 0-byte placeholder and its real content saved under a typo'd, undated filename instead | `docs/plans/2026-05-05-missing-report-features.md` (0 bytes) vs `docs/plans/panl mor missing features.md` (34KB, the actual content) | Low | **Done** | Makes the plans directory unreliable to search chronologically | Renamed the typo'd/undated file to the correct 2026-05-05-missing-report-features.md path (it had the real 975-line content); removed the stale 0-byte placeholder that was blocking the rename. |
| 33 | `docs/review.md` vs `docs/reviwe_reprot.md` (typo) — different content, misleading near-identical names | `docs/review.md` (Jenkins-pipeline audit notes) vs `docs/reviwe_reprot.md` (Jenkinsfile/backend payload compatibility analysis) | Low | **Done** | Not stale/contradictory, just easy to conflate or grep the wrong one — worth a rename for clarity | Renamed reviwe_reprot.md -> jenkinsfile-payload-compatibility-review.md and updated its 2 cross-references. |
| 34 | Two "UI/UX plan" docs are raw, unedited AI-chat pastes kept as permanent docs, one with a space in the filename | `docs/UI UX Improvement Plan.md`, `docs/UI_UX_plan.md` | Info | **Done** | Neither marked historical/superseded; the space in the filename is an operational hazard for naive shell scripts/globbing that don't quote paths | Renamed the space-containing filename to UI-UX-Improvement-Plan.md; added a historical-context banner to both UI/UX docs noting they're raw AI-chat pastes, not maintained specs. |

## Report Parsers

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 40 | Trivy parser drops findings across scan targets — dedup key is global, not per-target | `backend/app/services/reporting/parsers/trivy.py:103-113` (`vuln_by_id` declared outside the `Results[]` loop) | High | **Done** | If the same CVE appears in two different targets (e.g. `frontend/package-lock.json` and `backend/requirements.txt`), only the first survives — the second is silently discarded before ever becoming a finding. Real false negative in the tool's own parser |
| 41 | Trivy/Dependency-Check finding IDs don't include host/file — distinct vulnerable locations get merged into one Issue | `backend/app/services/reporting/parsers/trivy.py:149` (`id=f"TRIVY-{vuln_id}"`), `depcheck.py:184` (`id=f"DEPC-{vuln_id}"`); merge happens via `issue_service.py:16-42` (`_upsert` keys on `(issue_id, project_id)` only) | Medium-High | **Done** | Same CVE in `serviceA/package-lock.json` and `serviceB/package-lock.json` collapse to the same `issue_id` and overwrite each other's `host`/`location` on every ingest — whichever is processed last wins, the other location becomes invisible. Compounds #40 | Same fix already applied to trivy.py — id now includes file_name so distinct vulnerable files don't collapse into one Issue. New test confirms 2 distinct findings for the same CVE in 2 files. |
| 42 | ZAP parser reads fields that don't exist in ZAP's actual JSON report schema — `uri` always empty, multi-instance alerts collapse to one finding | `backend/app/services/reporting/parsers/zap.py:103-104` (`alert.get("uri", "")`, `alert.get("evidence", "")`) | High | **Done** | ZAP's real `jsonreport` format nests affected endpoints under `instances: [{uri, evidence, ...}]` per alert, not top-level `uri`/`evidence` keys — so those fields are always blank. Worse: a "Reflected XSS" found on 5 endpoints produces **one** finding with no indication 5 endpoints are vulnerable — triage can't tell which URL to fix |
| 43 | Severity mapping has silent holes — unrecognized severities either vanish from summaries or get silently downgraded | `backend/app/services/reporting/parsers/base.py:85-98` (`normalize_severity` defaults to `"Info"`), `sonar.py:56` (unmatched → `"Unknown"`, never counted in `calculate_severity_summary`) | Medium | **Done** | A Sonar finding with an unrecognized severity exists in `findings[]` but is invisible in every dashboard/counter driven by `severity_summary`. A Trivy `"UNKNOWN"` CVSS-less finding gets silently downgraded to `"Info"` rather than flagged as needs-review. No error, no log — easy to miss | normalize_severity now logs a warning on any unrecognized value (still defaults to Info, deliberately — changing the taxonomy is a bigger call). calculate_severity_summary now folds unrecognized severities into 'info' instead of silently dropping them, so totals stay accurate (sum(summary) == len(findings)). 2 new tests. |
| 44 | Nmap fallback finding ID is position-based and unstable across scans | `backend/app/services/reporting/parsers/nmap.py:135` (`f"NMAP-{finding.get('id', str(i+1).zfill(3))}"`) | Medium | **Done** | If the Jenkins-generated `findings.json` doesn't supply a stable `id`, the fallback is list index — reordering between scans (host added/removed, sort change) gives the same real finding a new ID, causing "old issue disappeared + new issue appeared" churn, and in the worst case a still-open port getting marked verified-fixed because its index shifted | Fallback finding ID now built from stable content (host/port/service/cve), not list position — same finding gets the same ID regardless of ordering. 2 new tests. |
| 45 | `parse_npm_audit_report` swallows malformed input silently instead of raising `ParseError` (currently dead code) | `backend/app/services/reporting/parsers/npm.py:56-60` | Low | **Done** | Every other parser raises `ParseError` on bad input, which `fetcher.py` catches and marks `parse_status="parse_error"` (a visible signal); this one returns `[]`, indistinguishable from "tool found nothing." Not biting today since neither `parse_npm_audit_report` nor `parse_nmap_xml` are wired into `fetcher.py`'s `TOOL_PARSERS` — both exported but unused. Latent trap if someone wires them up later expecting consistent error semantics | parse_npm_audit_report now raises ParseError on bad input like every other parser, instead of silently returning []. 1 new test. |

## Issue Lifecycle

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 46 | `IssueService.assign()` completely bypasses the state machine | `backend/app/services/issue_service.py:233-258`, called from `backend/app/api/issues.py:276-297` (`POST /issues/{issue_id}/assign`) | High | **Done** | Unlike `transition_status`, `assign()` never calls `is_valid_transition` — it force-sets `status="assigned"` from *any* current state (including `VERIFIED`/`FIXED`/`REJECTED`, all of which the state machine forbids transitioning to `ASSIGNED`), doesn't clear `resolved_at`, and hardcodes the "old status" in the audit history as `"open"` regardless of the real prior state. Concrete effect: a closed/verified security finding can be silently forced back into the active work queue by anyone with assign permission, with a corrupted audit trail and an inconsistent `resolved_at` (still set on a now-"assigned" issue) |
| 47 | Rejected issues have no regression path — `_upsert` never resets status, `detect_regressions` excludes `rejected` | `backend/app/services/issue_service.py:26-42` (`_upsert`), `backend/app/tasks/issue_tasks.py:250-269` (`detect_regressions` queries only `status.in_(["verified","fixed"])`) | Low/Info | **Agreed** | Defensible as "rejected = human already decided," but undocumented — a mis-triaged false-positive rejection is permanent unless someone manually notices and re-transitions it, even if the underlying finding's characteristics change on a later rescan. Worth confirming with the team this is deliberate |
| 48 | `project_grouping.deduplicate_findings` hash omits `tool` and `id` by design — can merge genuinely distinct findings, not just cross-tool duplicates | `backend/app/services/project_grouping.py:44-90` (`_compute_finding_hash`) | Medium | **Done** | Kept `tool`/`id` excluded from the main key (that exclusion is what makes true cross-tool CVE dedup work at all — Trivy and Dependency-Check report the same CVE with different `id`s). Added a targeted fallback: when a finding has *no* positional/identity signal whatsoever (host/cve/package/file all empty — the boilerplate-Sonar-text case this finding described), `tool` is appended to the key so two distinct no-signal findings from different tools no longer collide, while findings that do have real identity signal are unaffected and still cross-tool-dedup normally. Regression test: `tests/test_finding_dedup_hash.py::TestNoSignalFindingsFallBackToTool` — proves two generic same-tool findings still dedup (1 result) but two generic different-tool findings now stay distinct (2 results), and that findings with real signal (e.g. shared `host`) still dedup across tools despite the fallback logic being present. Full suite: 471 passed. |

## Positive findings — Report Parsers & Issue Lifecycle

| # | What | Location | Why it's good |
|---|------|----------|----------------|
| P23 | `fetch_sonar_issues` uses a request-scoped rule cache instead of a module-level global, specifically to prevent concurrent Sonar fetches from clobbering each other's rule lookups | `backend/app/services/reporting/parsers/sonar.py:210-213` | A real concurrency bug pre-empted with a clear comment explaining why |
| P24 | `detect_regressions` routes reopening through the state machine (`transition_status`) instead of writing `issue.status` directly | `backend/app/tasks/issue_tasks.py:262-268` | Keeps `resolved_at`/history consistent — the one regression-adjacent path that does this correctly, in contrast to finding #46 |
| P25 | ReDoS guardrails before running project-group naming patterns through `re.search` | `backend/app/services/project_grouping.py:117-123` (`calculate_match_confidence`) — pattern-length cap + nested-quantifier check | Shows real awareness that project-group patterns are semi-trusted input |
| P26 | Per-tool parse errors are non-fatal to the rest of the scan pipeline | `backend/app/services/reporting/fetcher.py:122-126` | A malformed report from one tool (`parse_status="parse_error"`) can't take down ingestion of the other four tools' reports |
| P27 | `transition_status` correctly enforces both the state machine and per-action RBAC together | `backend/app/api/issues.py:313-318`, `issue_service.py:275-278` | Verify/reject specifically gated behind `can_verify_issue`, separate from generic transition rights — the correct pattern finding #46 (`assign()`) should have followed |

## Jenkinsfile Pipeline Script & App Startup/Config

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 49 | Shell injection via `PROJECT.target_ip` in the Nmap stage — RCE on the Jenkins agent | `Agent/Jenkinsfile:809` | **Critical** | **Done** | `${PROJECT.target_ip}` is Groovy-interpolated into a single-quoted `echo '...'` shell string with only a non-blank check (no format validation). A crafted `target_ip` breaks out of the quoting and runs arbitrary shell on the Jenkins agent, which holds `CRED_CALLBACK`/`CRED_DOCKER` credentials for that job run. **Compounds #15** — `target_ip` is now confirmed as both an SSRF/credential-exfil vector AND an RCE vector. Fix: pass via `withEnv([...])` + single-quoted `sh '''...'''` referencing `$TARGET_IP`, the same safe pattern already used for `target_url` in the ZAP spider/active-scan calls a few hundred lines later |
| 50 | Same shell-injection pattern in the ZAP target-reachability `curl` check | `Agent/Jenkinsfile:853-856` | High | **Done** | Groovy-interpolates `${PROJECT.target_url}` into a single-quoted shell string one step before the code switches to the safe `withEnv`/`$TARGET_URL` pattern used later in the same stage — looks like an inconsistency/oversight, not a deliberate choice |
| 51 | NVD API key passed as a live CLI argument instead of stdin/env | `Agent/Jenkinsfile:382-407` (`argsWithKey = scanArgs + ["--nvdApiKey ${NVD_API_KEY}"]`) | Medium | **Open (needs your verification)** | Jenkins console masking scrubs it from log text, but it's still visible as a process argument (`ps aux`, `/proc/<pid>/cmdline`) to anything else on the same agent for the scan's duration. Inconsistent with the Docker Push stage a few hundred lines later, which correctly uses `docker login --password-stdin` — apply the same pattern here | Couldn't safely fix this one — `dependency-check.sh` isn't installed on this dev machine (it's a Jenkins-agent-only tool), so I can't verify which flag your installed OWASP Dependency-Check CLI version actually supports for keeping the NVD key out of process args (e.g. `--nvdApiKeyEnvironmentVariable`, if your version has it). Guessing at a flag for a live CI script felt like the wrong tradeoff — verify against `dependency-check.sh --help` on the actual Jenkins agent before changing this. |
| 52 | Only 1 of 9 scan stages has a Jenkins-side per-stage `timeout{}` block | `Agent/Jenkinsfile` — only "Sonar Scanner" (line 219) has one, matching backend's `STAGE_TIMEOUTS`; Dependency Check/Trivy FS/Docker Build/Docker Push/Trivy Image/Nmap have none | Medium | **Done** | These rely entirely on the single pipeline-wide `SCAN_TIMEOUT`. A hung Docker Build (e.g. a `RUN` step blocking on stdin) consumes the *entire* remaining budget instead of being killed at the backend-assumed 900s, starving all later stages — they silently never run, and the backend can't distinguish "stage X hung" from "pipeline is just slow" since it only sees the final callback | Added `options { timeout(...) }` (or a scripted `timeout{}` wrap for Git Checkout, matching Sonar Scanner's existing pattern) to all 7 previously-unbounded stages, matching backend's STAGE_TIMEOUTS values exactly. 8 of 9 stages now have a Jenkins-side timeout (Init Context doesn't need one — no external tool call). Verified brace/paren balance held before and after. |
| 53 | ZAP internal polling `curl` calls have no `--max-time`, unlike the reachability check a few lines earlier | `Agent/Jenkinsfile:916,926,993,1043,1059,1063` | Low/Medium | **Done** | `SPIDER_TIMEOUT`/`ASCAN_TIMEOUT` are only checked *between* loop iterations — if a single `curl` call itself hangs (ZAP daemon wedged, socket stays open), the loop never gets to re-check elapsed time. Combined with #52 (no Jenkins-side timeout on the ZAP stage), the only backstop left is the whole-pipeline timeout | Added `--max-time` to every curl call in the ZAP polling section (readiness checks, config calls, spider/ascan start, and critically the two status-poll calls inside the while-loops that could previously block SPIDER_ELAPSED/ASCAN_ELAPSED from ever being re-checked) and the report-fetch calls. |
| 54 | `Settings.DEBUG` is defined and validated but never actually wired to FastAPI — currently dead, but a footgun for whoever "fixes" it later | `backend/app/core/config.py:15,42-43`; confirmed unused via repo-wide grep, `main.py:26-28` never passes `debug=` | Info | **Done** | Harmless today. Risk is someone later wiring `debug=settings.DEBUG` into `FastAPI(...)` naively without preserving the existing staging guard, which would then leak tracebacks in a real deployment | Wired `debug=settings.DEBUG` into the FastAPI() constructor — safe specifically because config.py's validator already guarantees DEBUG can't be True outside dev/test, so this can't leak tracebacks in staging. |
| 55 | Schema-migration failures at startup are swallowed with `print()`, not logged or raised | `backend/app/main.py:242-247` (`except Exception as exc: print(f"Schema migration skipped/failed: {exc}")`) | Low/Medium | **Done** | In a containerized deployment where `logger` output is shipped to log aggregation/alerting but raw `print()`/stdout may not be (or is at different verbosity), a migration failure becomes invisible to on-call. A *partial* failure (3 of 6 `ALTER TABLE` statements silently fail) can leave the schema quietly diverged from what request-handling code expects, with no log signal pointing at the cause | Changed `print()` to `logger.error(..., exc_info=True)` — visible to log aggregation/alerting now, with full traceback. (Confirmed this is actually atomic under Postgres — engine.begin() wraps all statements in one transaction, so a mid-list failure rolls back everything rather than leaving a partial migration; the print()-vs-logger gap was the real, fixable issue.) |

## Positive findings — Jenkinsfile & App Config

| # | What | Location | Why it's good |
|---|------|----------|----------------|
| P28 | `shouldRun()` stage-selection is safe against injection | `Agent/Jenkinsfile:1184-1189` | `SELECTED_STAGES` parsed via Jenkins' sandboxed `readJSON` (not `evaluate`/`Eval.me`), and only checked against hardcoded literal stage names — no dynamic dispatch or code execution possible from a crafted stage list |
| P29 | Docker Hub push credential handling uses `--password-stdin`, not a CLI arg | `Agent/Jenkinsfile:588-632` | Correctly avoids leaking `DOCKER_PASS` into process args/shell history, with a comment explaining the backslash-escaping choice — this is the template finding #51 should follow |
| P30 | Git URL/branch validated against a regex + host allowlist before checkout, with an explicit, deliberate opt-out | `Agent/Jenkinsfile:174-189` (`^[a-zA-Z0-9._\/-]+$` branch regex, allow-listed host prefixes, `SKIP_GIT_WHITELIST` requires deliberate operator action) | Reasonable SSRF/injection mitigation — makes the *lack* of equivalent validation on `target_ip`/`target_url` (findings #15, #49, #50) stand out as a real gap rather than a stylistic choice |

## RBAC Service & Schema Validation

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 69 | `POST /api/v1/scans` (trigger a scan) has zero authentication | `backend/app/api/scans/routes.py:112-119` | **Critical** | **Done** | No `current_user` dependency at all — every sibling scan endpoint has one, this doesn't. An anonymous request with no cookie/JWT/API key creates a `ScanDB` row and fires a real Jenkins pipeline run against any project |
| 70 | `POST /scans/{id}/cancel` also has zero authentication, while its sibling `force-unlock` correctly requires admin | `backend/app/api/scans/state.py:28-33` vs `76-81` | High | **Done** | `cancel_scan` takes no `current_user` param at all; `force_unlock_scan` in the same file correctly uses `Depends(require_admin)`. Anonymous request terminates any in-progress scan for any project |
| 71 | Issue status transitions are only gated for verify/reject — every other target status has zero permission check, and the correct check already exists as dead code | `backend/app/api/issues.py:300-324` (`transition_issue`); `rbac_service.py:111-118` (`can_update_issue`, zero call sites) | High | **Done** | `if data.status in ("verified","rejected") and not can_verify_issue(...)` — but `open`/`in_progress`/`fixed`/`closed` transitions require no project access or assignee match at all. A developer with zero access to a project's issue can close it. **Needs a design decision before fixing**: is `can_update_issue()`'s existing rule (admin: any; team_lead: project-scoped; developer: only if assignee) the right policy, or should non-verify/reject transitions just require `has_project_access` like the list endpoints? Confirm before wiring `can_update_issue()` in |
| 72 | Project create/update/delete have no management-permission gate — only view-scope filtering | `backend/app/api/projects.py:176-286` | High | **Done** | `create_project` has no RBAC check at all. `update_project`/`delete_project` only apply `_filter_projects_by_user` (a *view* filter), not a "can manage" check — no such concept exists in `rbac_service.py`. A `developer` with mere view/triage access can `DELETE` a project, cascading to delete every scan row and `shutil.rmtree` its on-disk artifacts — irreversible, available to the lowest-privileged role |
| 73 | `POST /issues` (create_issue) has no RBAC or role check at all | `backend/app/api/issues.py:156-171` | Medium/High | **Done** | Meant for internal scan-ingestion use (`tasks/issue_tasks.py:136`) but exposed as a plain user-facing endpoint gated only by login. Any authenticated developer can POST a fabricated issue into any project's dashboard. Broader than #37 (which only covers the `project_id`-body-trust angle) — fixing this (add a real permission check, or restrict the endpoint to the service account and give end-users a narrower creation path if one's needed) closes #37 too |
| 74 | Service-account (`X-API-Key`) auth is unconditionally full admin | `backend/app/core/auth.py:34-47,86-97` | Medium | **Done** | The seeded `service-account` row has `role="admin"`, returned as-is for any request with a valid `X-API-Key` — intended for Jenkins/external scripts but far broader than scan-triggering needs. If that shared secret leaks (e.g. from Jenkinsfile config), the holder can promote any user to admin or delete accounts | Added `is_service_account` property and excluded it from `can_manage_users()` — the shared automation credential keeps full scan/project/report access (still needed) but can no longer manage users/roles/RBAC grants. 3 new tests. |
| 75 | `ProjectAccessCreate.scope_type` is an unvalidated raw string — a typo silently grants zero real access while reporting success | `backend/app/schemas/rbac.py:10-13`, `users.py:146-188` (`grant_project_access`) | Medium | **Done** | No `Literal["project","project_group"]` constraint. A wrong-case or typo'd `scope_type` (e.g. `"Project"`) is accepted, stored, and audit-logged as `scope_granted` — but `get_effective_project_ids()` filters via exact string match, so the row is silently inert. `RoleUpdate.role` has the same schema gap but is at least guarded by an explicit `validate_role()` call in the endpoint; `scope_type` has no equivalent | `scope_type` changed to `Literal["project", "project_group"]` — a typo'd value is now rejected by FastAPI request validation before it ever reaches the database. 3 new tests. |
| 76 | `ProjectUpdate` drops every field validator `ProjectCreate` has | `backend/app/schemas/project.py:52-59` | Low | **Done** | `ProjectCreate` enforces `http(s)://` on URLs, IPv4 regex on `target_ip`, length caps — `ProjectUpdate` redeclares the same fields as bare `Optional[str]` with zero validators. `PATCH` with a malformed `target_ip` succeeds where the equivalent `POST` would 422 — the origin point for values that eventually flow into the Jenkinsfile (see #49) | Extracted shared validator functions and applied them to `ProjectUpdate` too — a malformed target_ip/git_url/target_url via PATCH now 422s the same way POST does. 7 new tests. |
| 77 | `can_manage_project_access()` (team-lead scoped access management) is entirely dead code | `backend/app/services/rbac_service.py:90-96,179-191` | Info | **Done** | Zero callers — all access-management endpoints gate on admin-only `can_manage_users()` instead. Fails safe today (team leads simply can't use this via the API, more restrictive than presumably intended), but its `_user_has_access_to_same_scope` helper compares `scope_id` across `scope_type`s without filtering by type — worth fixing before anyone wires it up, not just wiring it up as-is | Fixed the latent bug in `_user_has_access_to_same_scope` (compared expanded project IDs against the target's raw unexpanded scope_id) by extracting a shared `_resolve_effective_project_ids` helper used on both sides. 2 new tests. Left the dead `can_manage_project_access` code in place (Info severity, zero callers) rather than deleting — now safe to wire up whenever that's decided. |

## Docker / Deploy Layer

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 78 | Jenkins container: Docker-socket mount + host networking + weak default password = near-trivial host root | `docker/docker-compose.jenkins.yml:8-19` (`network_mode: host`, `/var/run/docker.sock` bind-mounted, `JENKINS_ADMIN_PASSWORD:-admin123` fallback) | **Critical** | **Partially Done** | The credential half is fixed: `JENKINS_ADMIN_PASSWORD` now uses Compose's `:?` required-variable syntax — refuses to start at all without an explicit password, no more `admin123` fallback (verified via `docker compose config`). The Docker-socket mount and `network_mode: host` are left as-is deliberately: removing the socket would break the pipeline's `docker build`/`docker push` stages, and host networking may be relied on for reachability — both are real architecture tradeoffs that need your decision (e.g. rootless Docker-in-Docker, or accepting the risk behind a host firewall), not something to change unilaterally |
| 79 | `run.py down` merges dev+test+staging overlays and force-deletes volumes shared across all three | `run.py:56-77` (`run_down()`); none of the compose invocations set `COMPOSE_PROJECT_NAME`/`-p`, and dev/test/staging overlays declare identically-named volumes | High | **Done** | Each environment now gets its own Compose project name (`sentinel-dev`/`sentinel-test`/`sentinel-staging`) via `-p`, so their volumes are fully isolated from each other. `down` now takes `--env {dev,test,staging}` (defaults to `dev`, matching `start-dev.sh`'s existing unconditional call) and only tears down that one environment. Verified via `docker compose config` for all three; `AGENTS.md` updated to match |
| 80 | `.dockerignore` explicitly un-ignores `.env.test`, which then gets baked into the build context and image layers | `.dockerignore:13-14` (`!.env.test` negates the `.env*` exclusion above it); `config/.env.test` contains real-looking `JENKINS_TOKEN`/`CALLBACK_TOKEN`/`API_KEY` values; `docker/frontend.Dockerfile:9` does `COPY . .` with build context = repo root | Medium-High | **Done** | Removed the `!.env.test` negation. While fixing this, found the underlying `.env*` pattern was *also* silently ineffective for the real files — they live at `config/.env.dev`/`config/.env.test` (root `.env.dev`/`.env.test` are symlinks to them), and Docker's `.dockerignore` patterns without a leading `**/` only match at the build-context root, not subdirectories — so `config/.env.test` was landing in the image layer regardless of the negation line. Added `**/.env*` so env files are excluded at every depth. Verified directly: built the frontend Dockerfile's `builder` stage before and after the fix and ran `find /app -iname '.env*'` inside the resulting image — before, both `config/.env.test` and `config/.env.dev` were present; after, none were. `backend.Dockerfile` was never affected (it only `COPY`s the `backend/` subdirectory, which doesn't contain these files). Runtime `env_file:` usage in `docker-compose.test.yml` is unaffected — that's a host-filesystem read at container start, not part of the build context. |
| 81 | Hardcoded secrets checked into git and reused verbatim across dev/test/staging | `docker/docker-compose.dev.yml:44` (`VITE_API_KEY` hardcoded in the compose file), same value as `API_KEY` in `config/.env.test` and `setup-staging.sh:44`; Postgres password `"staging-password"` reused in **dev** too (`docker-compose.yml:10`, `docker-compose.dev.yml:6`) | Medium | **Left for you** | While fixing this, found it's worse than "same password, separate DBs": `docker-compose.dev.yml:6/17` and `docker-compose.yml:10/33` both point `DATABASE_URL` at the exact same host:port (`127.0.0.1:5433` / `localhost:5433`, `devsecops_staging`) — dev isn't just password-twinned with staging, it looks like it's running against the *same live staging database*, not a separate local instance. A real fix here means provisioning an actually-isolated dev Postgres (a new container + connection string) and rotating the shared credentials — both are infrastructure changes to a database you're actively using, not something I should do silently. Flagging with this detail so you can decide: spin up a real local dev DB, or confirm dev-against-staging is intentional (e.g. a shared low-stakes environment) and this is a documentation gap rather than a config bug. |
| 82 | All compose services use `network_mode: host`, bypassing container network isolation; Postgres/Redis exposed with weak/no auth | `docker-compose.yml:6,25,42,77` (backend/celery/frontend/sonarqube); Postgres `devsecops:staging-password` on 5433, Redis on 6380 with no `requirepass` at all | Medium | **Agreed** | Every container binds to all host interfaces, not just localhost. Nothing in the repo declares host-firewall rules — that's the only thing standing between the internet/LAN and Postgres/Redis. Compounds #78's blast radius if the host is ever exposed |
| 83 | `nginx.conf` has zero security headers and doesn't suppress version disclosure | `docker/nginx.conf:1-41` | Medium | **Done** | Added `server_tokens off;` plus `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, and a CSP (`script-src 'self'` — the app has no inline scripts; `style-src` allows `'unsafe-inline'` since Tailwind/recharts rely on inline `style` attributes at runtime; `connect-src` covers same-origin API/WebSocket traffic through this same nginx instance; `frame-ancestors 'none'` covers the same clickjacking protection as `X-Frame-Options` for CSP-aware browsers). Deliberately left out HSTS — this nginx currently serves plain HTTP only (no TLS termination here per the existing host-network setup), and `Strict-Transport-Security` is a no-op over HTTP but can be a footgun if this ever moves behind a domain without TLS everywhere; add it once TLS termination is actually in place. Verified by building the real `frontend.Dockerfile` image, running it, and curling it: confirmed all four headers present, `Server: nginx` (no version) instead of `Server: nginx/<version>`, static assets and `index.html` still serve with 200s (no inline `onclick=`/`onerror=` attributes in the shipped `index.html` that the strict `script-src` would have broken). |
| 84 | Base images are unpinned/floating | `docker/backend.Dockerfile:1` (`python:3.11-slim`, no patch/digest pin); `docker/frontend.Dockerfile:2,13` (`node:20-alpine`; production stage is bare `nginx:alpine`, **no version at all**) | Low-Medium | **Done** | Pinned all three base images by digest (`image:tag@sha256:...`, resolved from the currently-published `3.11-slim`/`20-alpine`/`alpine` tags) — the strongest form of pinning, immune to the tag being re-pointed at new content entirely, not just a patch-version pin. Left a comment on each noting the digest should be re-resolved and bumped periodically to pick up base-image security patches, since digest pinning trades auto-patching for reproducibility. Verified both Dockerfiles still build clean end-to-end with the pinned digests (`docker build` succeeded for both `backend.Dockerfile` and the full multi-stage `frontend.Dockerfile`, including the `nginx:alpine` production stage). |
| 85 | `docker/jenkins/tools.sh` downloads ZAP/Trivy release tarballs via `wget` with no checksum/signature verification | `docker/jenkins/tools.sh:19-35` | Low-Medium | **Done** | Added a `verify_sha256()` check after each `wget`, exiting non-zero on mismatch before extraction. Real per-file sha256 digests for the exact pinned versions (ZAP 2.17.0, Trivy 0.70.0) were pulled from GitHub's own Release API `digest` field (`GET /repos/<org>/<repo>/releases/tags/v<version>`, per-asset `digest` key — this is GitHub's own computed hash, not a fabricated value), documented in-script with the exact retrieval method so they can be correctly re-derived on future version bumps. Verified the shell logic directly: `bash -n` syntax check passes, and a standalone test of `verify_sha256()` confirmed it accepts a matching checksum and exits 1 with a clear error on a mismatched one. Could not fully round-trip by re-downloading the actual release tarballs from inside this sandbox (network egress here reaches `api.github.com` but times out against `release-assets.githubusercontent.com`, the actual asset CDN) — the digests are trusted as sourced directly from GitHub's API rather than independently re-verified byte-for-byte in this environment. |

## Positive findings — RBAC/Schema & Docker/Deploy

| # | What | Location | Why it's good |
|---|------|----------|----------------|
| P40 | `UserCreate` only exposes `username`/`password` — no `role` field reachable from `/register` | `backend/app/schemas/user.py`, `db_models.py:66` (server-side default `"developer"`) | Solid mass-assignment protection on the endpoint most likely to be targeted for privilege escalation at signup |
| P41 | `can_approve_rescan`/`can_assign_issue`/`can_verify_issue` are internally consistent and correctly layered (admin → team_lead-with-access → deny) | `backend/app/services/rbac_service.py:101-138` | Every endpoint that should call one of these does call the correct one before mutating state — makes the *other* findings (#69-73, endpoints that skip this pattern entirely) stand out as the exception, not the norm |
| P42 | `get_current_user`'s auth fallback chain (bearer → cookie → API key → reject) special-cases the callback route via strict path matching, not a prefix check | `backend/app/core/auth.py` (`_is_callback_route`) | Avoids an auth-bypass-by-path-confusion bug that a naive `path.startswith(...)` check would risk |
| P43 | `.dockerignore` is otherwise well-scoped | `.dockerignore` | Excludes `.git`, `node_modules`, `.venv`, `__pycache__`, `dist`, `build`, `coverage`, generic `.env*` — the `.env.test` carve-out (#80) is a one-line mistake, not a systemic gap |
| P44 | Both Dockerfiles actually run as non-root, confirmed wired not just declared | `backend.Dockerfile:17-18` (`appuser`), `frontend.Dockerfile:25-34` (chowns nginx runtime dirs, switches to `nginx` user) | |
| P45 | Multi-stage builds correctly minimize final image contents | `frontend.Dockerfile:19` (production stage only copies `/app/dist`) | No `node_modules`/source in the shipped image |
| P46 | Backend image proactively strips `.pyc`/`__pycache__` after `pip install` | `backend.Dockerfile:9-11` | Small but real image-size/attack-surface reduction most Dockerfiles skip |
| P47 | `nginx.conf` correctly denies dotfile access, no directory listing | `nginx.conf:37-40` (`location ~ /\. { deny all; }`), no `autoindex on` anywhere | |
| P48 | Deploy shell scripts consistently fail loud instead of continuing silently | `setup-staging.sh:18`, `start-dev.sh:6` (`set -e`); `jenkins/tools.sh`, `migrate.sh` (`set -euxo pipefail`/`set -euo pipefail`) | None of the shell scripts reviewed silently continue past a failed step |
| P49 | `migrate.sh`'s volume swap is defensively written with an explicit rollback procedure | `docker/jenkins/migrate.sh:41-42` (`docker volume rm ... \|\| true` before recreate) | Avoids a hard failure if the volume doesn't yet exist, prints rollback steps at the end |

## UI Consolidation / Technical Debt

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 86 | Three report pages cover overlapping territory and don't even share layout consistently | `src/pages/ProjectReportsPage.tsx` ("Scan Report"), `src/pages/UnifiedReportPage.tsx` ("Security Report"), `src/pages/DeveloperReportPage.tsx` ("Developer View") | Medium (tech debt) | **Agreed** | `ProjectReportsPage` and `DeveloperReportPage` both render through the shared `ProjectReportLayout` (which has the tab bar linking all three views); `UnifiedReportPage` builds its own standalone header/back-button instead of using that layout. Result: the tab bar is present on 2 of 3 views and silently disappears on the third — inconsistent shell, not three deliberately different UIs. Candidate for consolidating into one page + `ProjectReportLayout` tab content instead of three separate page components |
| 87 | Two dashboard pages fetch the identical backend data (`portfolio-overview`), suggesting real content overlap rather than distinct aggregation levels | `src/pages/DashboardPage.tsx:65-70`, `src/pages/PortfolioDashboardPage.tsx:189-193` (both `queryKey: ["portfolio-overview"]`) | Medium (tech debt) | **Agreed** | 5 total nav-level dashboard destinations exist (`Dashboard`, `Security Overview`/Portfolio, `Trends`, `Team Workload`, `Executive Summary`). `ExecutiveSummaryPage` explicitly states its headline PDF-export feature "is not available yet" — reads as an unfinished parallel effort rather than a deliberately separate view. Worth scoping whether Dashboard/Portfolio should merge into one page with a toggle before adding anything further to either |

## Found During Fix Pass

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 88 | `PATCH /projects/{id}` 500s whenever the project has no `branch` set — response never defaults it to `"main"` the way `GET /projects/{id}` does | `backend/app/api/projects.py` (`update_project`) | Low | **Done** | Discovered via a new regression test for #72; `project_data` was built directly from `db_project.__dict__` without the `or "main"` fallback `get_project` applies. Fixed by mirroring that default in the PATCH response too |
| 89 | `cancel_rescan_request` tried to transition `PENDING_VERIFICATION -> FIXED`, which the state machine doesn't allow — 500'd whenever a requester cancelled a pending-verification rescan | `backend/app/api/issues.py` (`cancel_rescan_request`) | Medium | **Done** | Found while implementing the real reject-rescan endpoint for #56 (which copied the same broken pattern before being caught by a new regression test). Fixed both to transition to `IN_PROGRESS` instead, which is both a valid transition and the semantically correct target |
| 90 | `safe_broadcast`/`ConnectionManager.broadcast_event` — coroutine never awaited (`RuntimeWarning: coroutine 'ConnectionManager.broadcast_event' was never awaited`) | `backend/app/websockets/manager.py:16` (`safe_broadcast`) | Low | **Done** | Surfaced as a warning during the #56 test run. `broadcast_event` is `async` but `safe_broadcast` calls it from sync code without awaiting or scheduling it — the broadcast silently never happens. Not investigated further; flagging for a later pass | Fixed via `asyncio.run()` in `safe_broadcast` (no caller has a running event loop of its own). New `tests/test_websocket_broadcast.py` asserts the coroutine is actually awaited; confirmed the warning no longer appears in the full suite run. |
| 91 | Live GitHub Personal Access Token embedded in cleartext in `.git/config` | Local git config only (`git remote -v` shows `https://ghp_****@github.com/AI-Deamon/Pipeline.git` — token redacted here deliberately) | **Critical** | Open | Not a tracked file, so it wouldn't show up in the earlier file-content secret scans — but it's a real, live credential sitting in cleartext on disk. Anyone with local file access, a leaked backup, or access to this machine's home directory has push/pull access to this repo (and whatever else that PAT is scoped to) on your GitHub account. **Not something to fix autonomously** — rotate/revoke this token in GitHub settings yourself, then re-set the remote via SSH or a credential helper instead of embedding it in the URL |
| 92 | `npm run lint` currently fails on 23 pre-existing errors, entirely in `e2e/` | `e2e/*.spec.ts` — unused vars (`@typescript-eslint/no-unused-vars`), `prefer-const`, `@typescript-eslint/no-explicit-any` | Low | **Done** | Found while wiring up #64's new CI workflow — intentionally left lint out of that workflow rather than ship a check that's red from day one. Worth a cleanup pass before adding `npm run lint` as a CI gate | Fixed all 23 (unused imports/vars, prefer-const, no-explicit-any, one useless try/catch) across 8 e2e spec files — `npm run lint` is now fully clean. Verified via `npx playwright test --list` (178 tests across 19 files still parse/load correctly). Re-enabled the lint step in #64's CI workflow. |

| 93 | An entire in-memory scan-state subsystem is dead code, still imported into a live file via a stale type hint | `backend/app/models/scan.py` (`Scan` class), `backend/app/state/persistence.py` (`persist_state`/`restore_state`), `backend/app/services/scan_orchestrator.py` (`orchestrate_scan` is an explicit no-op, `create_scan_object` has zero callers) | Low (tech debt) | **Agreed** | Found while scoping #65. All three files are orphaned from an earlier pre-DB architecture phase — zero real callers anywhere except each other. `jenkins_service.py::trigger_scan_job(scan: Scan, ...)` still type-hints the dead `Scan` class, but the real call site (`scans/routes.py::trigger_verify_scan`) actually passes a `ScanDB` SQLAlchemy instance — it only "works" because both classes happen to share attribute names (duck typing, no runtime type enforcement). Candidate for deletion, but didn't remove unilaterally — flagging for your call since it's a repo-cleanup decision, not a bug fix |

## Concurrency & Race Conditions

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 94 | `cancel_scan`/`force_unlock_scan` never lock the row — a concurrent Jenkins callback (or recovery sweep) can be silently overwritten | `backend/app/api/scans/state.py` (`cancel_scan`, `force_unlock_scan`) | High | **Done** | Neither uses `with_for_update()`, unlike `callback.py`/`scan_recovery.py` which both explicitly document why they do. Sequence A (callback race): scan is RUNNING → admin calls cancel, reads state=RUNNING, passes the terminal-state check → Jenkins fires the completion callback concurrently, locks the row, transitions to COMPLETED, commits, dispatches the issue-migration Celery chain → the cancel request's queued UPDATE (no `WHERE state=...` guard) then fires and blindly overwrites `state=CANCELLED` over the just-committed COMPLETED row. Sequence B (recovery race, confirmed by a second independent review pass): the timeout-based recovery sweep marks the scan FAILED and commits first; the blocked cancel/unlock request then lands and unconditionally overwrites it with CANCELLED using error info computed before it ever saw the lock. Either way, downstream processing (issue migration, verification) proceeds against a DB record that no longer reflects what actually happened to the scan |
| 95 | `rescan_service.update_with_version_check` is not an atomic compare-and-swap — lost updates under real concurrency | `backend/app/services/rescan_service.py:71-86`; `RescanRequestDB.version` (`db_models.py:286-308`) is a plain `Integer` column, not a SQLAlchemy `version_id_col` | High | **Done** | The version check is a Python-level `if record.version != new_version: raise` before flush — the resulting UPDATE has no `WHERE version=...` clause. Two concurrent `PATCH /rescan-requests/{id}` requests both reading `version=3` under READ COMMITTED both pass the check and both flush unconditionally; no 409 ever raised, last-committed silently wins. Defeats the purpose of the version field for genuinely concurrent requests (only sequential requests get real protection) |
| 96 | Cache invalidation in `_record_history` runs before the transaction commits — a concurrent read can repopulate the cache with stale data that then survives the update | `backend/app/services/issue_service.py:213-231` (`cache_delete`/`cache_delete_pattern` called right after `flush()`, before the caller's later `commit()`) | High | **Done** | Sequence: `PATCH /issues/{id}` flushes a status change and invalidates the cache, but hasn't committed yet → a concurrent `GET /issues/{id}` hits the now-empty cache, reads the DB under READ COMMITTED and sees the pre-update row (writer hasn't committed), re-populates the cache with stale data and a 60s TTL → writer commits. Cache now holds wrong data for up to 60s (5s for the pending-verification list) with nothing left to invalidate it again |
| 97 | Concurrent `migrate_scan_to_issues` for the same project+tool can poison the SQLAlchemy session and silently drop findings | `backend/app/tasks/issue_tasks.py:110-151`, `IssueService._upsert` (`issue_service.py:12-72`), unique index `ix_issues_issue_id_project` | Medium | **Done** | Wrapped each finding's `service.create_issue(db, issue_data)` call in `with db.begin_nested():` (a SAVEPOINT). Previously a mid-loop `IntegrityError` left the whole outer transaction poisoned in SQLAlchemy's session state (independent of DB backend — confirmed this by reproducing the exact "Session's transaction has been rolled back due to a previous exception during flush" error against the SQLite test DB), silently skipping every finding processed after the failure and leaving `report.migration_status` stuck at `"processing"` since the final commit also then fails. The SAVEPOINT scopes a failed flush to just that one finding, letting the loop continue normally. Regression test: `tests/test_migrate_scan_savepoint.py` — bypasses the check-then-insert existence check for one finding to flush a genuine duplicate against the real unique `(issue_id, project_id)` index (simulating the real race), then asserts the findings before/after it in the same loop still get created and `migration_status` ends `"completed"`. Verified this test actually fails without the fix (reverted the `begin_nested()` wrap, confirmed the test failed with `migrated: None` and every remaining finding skipped, then restored the fix). Full suite: 474 passed. |
| 98 | `ConnectionManager` broadcast methods iterate a live mutable connection set across `await` points that `disconnect()` mutates concurrently — real `RuntimeError`, not just a GIL non-issue | `backend/app/websockets/manager.py:77-123` (`broadcast_to_scan`/`broadcast_to_project`/`broadcast_global` iterate `self.scan_connections[...]` directly, not a copy) | Medium | **Done** | All three broadcast methods now iterate `list(self.<...>_connections[...])` — a snapshot — instead of the live set, so `disconnect()`'s concurrent `.discard()` can't raise `RuntimeError: Set changed size during iteration` mid-broadcast. Also hardened `send_scan_update`: each of the three tiers (scan/project/global) is now individually try/excepted so one tier's failure no longer skips the remaining ones. Regression test: `tests/test_websocket_manager_concurrency.py` — a fake connection's `send_json` disconnects a sibling connection mid-broadcast (reproducing the real race), asserted not to raise; verified this actually catches the bug by reverting the `list(...)` copy and confirming the test fails with the exact `RuntimeError`, then restored the fix. A second test confirms `send_scan_update` still delivers to the project/global tiers when the scan tier raises. Full suite: 474 passed. |
| 99 | `rbac_service.get_effective_project_ids()`'s two-query group expansion isn't in one atomic snapshot | `backend/app/services/rbac_service.py:55-84` | Info | **Agreed** | An assignment change between the direct-project query and the group-membership query could make the returned set briefly inconsistent with any single instant. Low impact — only affects what one already-authenticated request sees for itself, not a cross-user leak. Not worth fixing as a security bug, only noted for correctness-under-churn completeness |

## Remaining Frontend Pages

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 100 | Stale-closure race lets a fast group switch overwrite the currently-viewed group's data | `src/pages/ProjectGroupsPage.tsx:103-131` (`handleAutoAssign`, `handleRefreshGroup`) | High | **Done** | `selectedGroup` is captured from the render when the button was clicked, not re-read after the `await`. Click Auto-Assign on Group A, then switch to Group B before A's request resolves → when A resolves, the stale closure's `selectedGroup.group_id === "A"` check still passes, silently replacing B's displayed data with A's while the header still reads "B". Same bug in `handleRefreshGroup`. Needs a ref or request-id/AbortController guard instead of comparing captured state |
| 101 | `CreateProjectPage`'s validation never checks step 3 fields at all — `validateStep(3)` always returns true | `src/pages/CreateProjectPage.tsx:54-74` (`validateStep`) | High | **Done** | Only branches on `currentStep === 1`/`=== 2`; `target_ip`/`target_url` sail through all three wizard steps unchecked even though the backend enforces an IPv4 regex and `http(s)://` prefix. User gets a raw 422 toast after seemingly completing the wizard, no field/step indication, no auto-navigation back to step 3. `ProjectEditPage`'s `ProjectForm.tsx:56-66` already has the correct mirrored validation — `CreateProjectPage` just never adopted it |
| 102 | `CreateProjectPage`'s git_url client validation is looser than the backend's | `src/pages/CreateProjectPage.tsx:60-69` (`new URL(...)` accepts any scheme) vs `backend/app/schemas/project.py:27-34` (`^https?://` required) | Medium | **Done** | Replaced `new URL(...)`'s any-scheme check with the same `^https?://` requirement (plus the 2048-char length cap) the backend's `_validate_git_url_value` enforces. Found and fixed the identical gap in `ProjectForm.tsx` (used by the project-edit flow) too, since it had the exact same loose `new URL(...)` check. Regression test: `src/tests/pages/CreateProjectPage.gitUrl.test.tsx` — an `ssh://` URL is now rejected at step 2 with the scheme-specific error and never advances to step 3; a valid `https://` URL still passes through. Full frontend suite: 133+ passed, `tsc -b` clean. |
| 103 | An auto-added "required" dependency stage can become permanently stuck and unremovable | `src/pages/ManualScanPage.tsx:37-63` (`handleStageToggle`) | Medium | **Done** | Fixed both #103 and #104 together with the same rewrite, since they're two symptoms of the same root design flaw (an incrementally-patched `autoStages` Set). Replaced it entirely: `manualStages` is now the only state (the user's actual picks), and `autoStages` is a pure value recomputed fresh from it via a new `computeAutoStages()` (`src/utils/scanStages.ts`, extracted to its own module per the repo's fast-refresh lint rule) on every render — a stage is "auto" exactly when it's currently required, transitively, by some other manually-selected stage. Deselecting always recomputes from scratch, so a shared dependency can no longer get stuck forever. Regression tests: `src/tests/pages/ManualScanPage.test.tsx` — unit tests on `computeAutoStages` prove a dependency only needed by the deselected stage is fully freed (the literal #103 scenario) while one still needed by another manually-selected stage correctly stays required; a UI-level test drives real clicks and asserts a dependency stage becomes clickable again after its only activator is deselected. Verified both fail against the original buggy implementation (temporarily restored it, confirmed the exact `cursor-default`-stuck-forever failure, then restored the fix) and pass against the fix. Full frontend suite: 139 passed, `tsc -b`/lint clean. |
| 104 | `setSelectedStages` updater has impure side effects (`setAutoStages` calls) inside it | `src/pages/ManualScanPage.tsx:37-63` | Low | **Done** | Fixed as part of #103's rewrite (see that entry) — `handleStageToggle` is now a plain, side-effect-free `setManualStages(prev => ...)` call; `autoStages` is a `useMemo`, not a second piece of state a nested updater could double-apply to. |
| 105 | No self-delete protection or warning on the user-management delete flow | `src/pages/UserManagementPage.tsx:213-222` | Medium | **Done** | Added `useAuth()`'s `currentUser` to the page and changed the Delete button's guard from `user.username !== 'admin'` to `user.username !== 'admin' && user.id !== currentUser?.id` — the caller's own row now never shows a Delete button at all, regardless of their username or role, rather than relying on a warning after the fact. Regression test: `src/tests/pages/UserManagementPage.test.tsx` (new describe block) — mocks `useAuth` as `lead1` (not literally "admin") and asserts their own row has no Delete button while `dev1`'s still does; verified this fails against the original `username !== 'admin'`-only guard (temporarily reverted it, confirmed the exact assertion failure, then restored the fix). Full suite: 7/7 passed in this file, `tsc -b`/lint clean. |
| 106 | `TrendAnalysisPage`'s aggregated trends query key doesn't include the project list, so it won't refresh when projects change | `src/pages/TrendAnalysisPage.tsx:248-292` (`queryKey: ["trends", "all"]`, `queryFn` closes over `projects` from a separate query) | Low/Medium | **Done** | Added a `projectIdsKey` (`useMemo`-derived, sorted joined project-id string) to the trends query's key: `["trends", "all", projectIdsKey]`. TanStack Query now treats a changed project set as genuinely different data instead of the same stale cache entry. Regression test: `src/tests/pages/TrendAnalysisPage.queryKey.test.tsx` — a project added after mount (via the same `api.projects.list` mock resolving different data on a later call, matching how the real function behaves) gets folded into the trends aggregate without a remount; verified this fails without the fix (reverted the key, confirmed the new project's trends were never fetched, then restored the fix). Full frontend suite: all assertions passed across repeated runs (a pre-existing, unrelated vitest worker-pool flake intermittently kills a random test file under load — same known issue noted earlier this session — but no actual test failure). |
| 107 | "Current vs previous" trend comparison can mislead when projects report on different dates | `src/pages/TrendAnalysisPage.tsx:270-289,312-318` | Low | **Done** | A true fix (per-project date alignment so "current" only compares same-date data) is a bigger aggregation redesign, so instead surfaced what's actually being compared: a caveat line above the summary cards now shows the real two dates being diffed (e.g. "Comparing Feb 15 to Jan 15 — each date reflects whichever projects had scan data on that day, not all projects necessarily"), so a reader can judge staleness/coverage themselves instead of the numbers silently implying same-day parity across every project. Regression test: `src/tests/pages/TrendAnalysisPage.test.tsx` — asserts the caveat renders with the correct formatted dates from the fixture data. Full frontend suite: passed (see #106's entry for the same run). |

## Positive findings — Concurrency & Frontend

| # | What | Location | Why it's good |
|---|------|----------|----------------|
| P50 | `scan_recovery.py`'s two-phase lock pattern correctly closes the gaps it's meant to | `poll_jenkins_for_active_scans`/`recover_stuck_scans` (`scan_recovery.py:151-249`) | Prefetches unlocked IDs (avoids holding locks across a slow Jenkins HTTP call), then re-locks and re-verifies state is still non-terminal before mutating, commits per-row to release locks promptly. No gaps found |
| P51 | `callback.py`'s row lock + idempotency-digest combo correctly handles concurrent/retried Jenkins deliveries | `callback.py:142-169` | The correct pattern that #94 (cancel/unlock endpoints) should have copied but didn't |
| P52 | `cache.py` is genuinely Redis-backed, not an in-process dict — doesn't share the multi-worker problem already documented for the rescan rate limiter | `backend/app/services/cache.py:19-32,71-79` | Degrades gracefully to no-op on connection failure; `cache_delete_pattern` correctly uses non-blocking `SCAN` rather than `KEYS` |
| P53 | Verify/Reject buttons on rescan cards are disabled on either pending flag, not just their own | `src/components/RescanRequestCard.tsx:64,74` | Prevents a double-submit race where both actions could fire concurrently on the same request |
| P54 | `ProjectEditPage` blocks edits while a scan is active | `ProjectEditPage.tsx:85,107-108` (`isLocked` from `last_scan_state`) | Good guard against mutating project config mid-scan |
| P55 | `ProjectForm.tsx`'s client-side `target_ip`/`target_url` validation exactly mirrors the backend's regexes | `ProjectForm.tsx:56-66` | The correct pattern #101/#102 (`CreateProjectPage`) should have copied |
| P56 | `ScanHistoryPage` distinguishes "no scans yet" from "no search matches," and gates the destructive force-stop action behind a real confirm modal | `ScanHistoryPage.tsx:75-91` | Not a generic empty state, not a fire-immediately destructive action |
| P57 | `UserManagementPage`'s admin-permission gate re-checks reactively via the effect's dependency array | `UserManagementPage.tsx:34-41` | If the current user's role/permissions change underneath them, they're redirected out rather than continuing to see stale admin controls |

## Issue Lifecycle — Self-Audit & Fresh Bugs

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 108 | **Regression I introduced this session**: `assign()`'s state-machine fix (#46) blocked reassigning an `IN_PROGRESS` issue to a different developer | `backend/app/services/issue_service.py` (`assign()`) | High | **Done** | The original #46 fix only special-cased "already ASSIGNED" as a skip-transition reassignment case; any other from-state (including the very common IN_PROGRESS handoff workflow) went through `is_valid_transition(IN_PROGRESS, ASSIGNED)`, which is False — hard 400 error on a previously-working reassignment. Fixed by treating both `ASSIGNED` and `IN_PROGRESS` as "already active, just update assignee, don't force a status change." New regression test `test_reassign_in_progress_issue_does_not_raise` added; full suite green (410 passed) |
| 109 | `auto_verify_fixed_issues` bypasses the entire reviewer-approval verification workflow — fires on every completed scan stage, not just approved verification scans | `backend/app/tasks/issue_tasks.py:183-228`, chained unconditionally from every scan in `callback.py:115-125` | High | **Done** | Any issue with `status in (fixed, pending_verification)` gets auto-transitioned to `verified` if simply absent from *any* scan of that project/tool — no requirement that this be the reviewer-approved verify-scan. A developer marks an issue fixed and requests a rescan (creating an unapproved `pending` `RescanRequestDB`); before a reviewer approves it, an unrelated regular/scheduled scan runs and — due to tool nondeterminism or scope change — simply doesn't redetect the finding. `auto_verify_fixed_issues` marks it `verified` outright, completely sidestepping the `pending_verification`/`approve_rescan`/RBAC-gated review model built elsewhere in this same codebase | Fix: removed "pending_verification" from `auto_verify_fixed_issues`'s status filter — it now only touches plain "fixed" issues, which is a legitimate direct FIXED->VERIFIED transition per the state machine when a routine scan confirms absence. Issues formally in the review queue (`pending_verification`) are now only ever resolved by `auto_verify_pending_rescans` (already correctly gated on an approved RescanRequestDB linked to that specific verify-scan) or a manual approve/reject. New regression test confirms a pending_verification issue is left untouched by an unrelated scan. |
| 110 | `auto_verify_pending_rescans` can crash mid-batch on a state conflict, orphaning approved rescans and rolling back unrelated ones in the same batch | `backend/app/tasks/issue_tasks.py:283-370` (`_process_rescan_verification`) | Medium/High | **Done** | Wrapped each `_process_rescan_verification` call in a per-rescan try/except that logs and continues instead of letting the `ValueError` (invalid transition) propagate to the outer handler. Previously that exception aborted the whole batch — every other rescan already processed in the same call, for unrelated issues, got rolled back with it (only one final `db.commit()` exists for the whole loop). Now one stale/conflicting rescan is skipped and logged while the rest of the batch still commits normally. Left as a known residual gap, not fixed here: the skipped `RescanRequestDB` still stays `approved`/unlinked with nothing to automatically re-queue it — that's a separate, lower-severity cleanup concern (a stuck-approved-rescan sweep) rather than the batch-abort bug this finding was about. Regression test: `tests/test_auto_verify_pending_rescans_batch.py` — two issues in one batch, one already in a conflicting `verified` state; asserts the healthy issue's rescan still completes and commits despite the conflicting one failing. Verified this test fails without the fix (reverted the try/except, confirmed the whole batch errored with nothing committed, then restored the fix). Full suite: 475 passed (with stable test ordering — `pytest-randomly` occasionally shuffles order across unrelated pre-existing test files in a way that causes flakiness unrelated to this change, confirmed by reproducing the same flakiness against the pre-session baseline). |
| 111 | `migrate_scan_to_issues`'s fallback issue ID (embeds the current `scan_id`) guarantees non-deduplicated, ever-growing issue churn for any finding lacking a stable parser-supplied ID | `backend/app/tasks/issue_tasks.py:75-97` (`_build_issue_data`), `110-151` | Medium | **Done** | Added `_stable_fallback_issue_id()` — same content-hash approach as the already-fixed Nmap-specific version (#44), generalized to any tool: hashes `tool_name` + rule/title + normalized file/line into a stable 16-char digest, used only when the parser didn't supply a real `id`/`issue_id`. The same finding now produces the same fallback ID across scans (so `_upsert` correctly matches it to the prior row and updates `last_seen_at` instead of creating a duplicate), and is no longer affected by `count` or by unrelated earlier failures in the same batch shifting position. Regression test: `tests/test_stable_fallback_issue_id.py` — same finding across two different `scan_id`s produces the same ID; the same finding at `count=0` vs `count=5` also produces the same ID (proving position/prior-failure independence); different findings still get different IDs; a parser-supplied `id` still takes priority over the fallback. Full suite: 479 passed. |

## Reporting & Aggregation

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 112 | Group rollup aggregates every historical scan ever matched to a group, not just each project's latest — stale/fixed findings never age out | `backend/app/services/project_grouping.py:141-161` (`find_matching_scans`, no per-project "latest only" filter), `184-223` (`auto_assign_group_scans`, dedupes only same scan_id twice, not multiple scan_ids of the same project), `236-280` (`get_group_aggregated_report`) | High | **Done** | Project scanned Jan 1 (critical CVE found) and rescanned Feb 1 after patching (CVE gone) — both scan_ids match the group and get assigned; the aggregated report still includes the Jan 1 CVE forever, since `deduplicate_findings` only collapses hash-identical findings *within* the merged set, it can't detect "absent from the newer scan." A group dashboard overstates risk indefinitely for any project scanned more than once | Fix: `get_group_aggregated_report` now keeps only the latest `ScanReportDB` per `(project_id, tool_name)` among the group's assigned scans, mirroring the exact pattern `portfolio.py`'s overview already used correctly (P60). 2 new regression tests: the exact Jan-1-CVE/Feb-1-patched scenario from the finding now correctly shows 0 criticals, and a sanity check that two different tools' latest reports for the same project are both still kept (not accidentally collapsed to one). |
| 113 | Risk-scoring formula is inverted — high-volume medium/low noise can outscore an actual critical finding | `backend/app/services/reporting/risk_calculator.py:11-32` (`RiskCalculator.WEIGHTS`/`calculate`, linear subtractive: `100 - 15*critical - 10*high - 5*medium - 1*low`), duplicated inline in `backend/app/api/portfolio.py:110` | High | **Done** | Traced with real numbers: 1 critical + 5 medium → score 60 → "Medium Risk". 0 critical + 50 medium (pure noise volume) → score 0 (floored) → "Critical Risk". A project with a genuine unpatched critical CVE reads as *less* risky than a project with zero criticals but lots of medium-severity code-smell noise — the opposite of what a security team needs. Also: a single critical alone (score 85) still lands in "Low Risk" (≥80 threshold) — a dashboard showing "Low Risk" next to a live critical CVE is a serious sanity failure for a security tool | Redesigned with a tiered-ceiling formula: the highest severity tier present (critical/high/medium) sets a hard ceiling on the achievable score (critical -> capped at 39, forcing "Critical Risk" regardless of volume elsewhere), and within-tier penalty uses sqrt(count) diminishing returns instead of an unbounded linear multiple. Re-traced the exact numbers from the finding: 1 critical + 5 medium now scores 14 ("Critical Risk"), 0 critical + 50 medium now scores 58 ("High Risk") — critical correctly dominates. A lone critical alone now scores 21, always "Critical Risk" (was 85, "Low Risk"). Also fixed portfolio.py's separately-duplicated, already-drifted copy of the old formula to call the shared calculator instead. 5 new regression tests added proving the invariants directly, not just loose 0-100 bounds. |
| 114 | Portfolio overview presents severity data from different scan runs under one unified "last scan" timestamp | `backend/app/api/portfolio.py:28-104` (each tool's severity comes from that tool's own latest `ScanReportDB`, independent of scan_id) vs `:86-104` (last-scan-time comes from a separate `ScanDB` query) | Medium | **Done** | Backend-side fix, additive only (no existing field changed, so no frontend breakage): added `severity_as_of` to each project's portfolio entry — the oldest `created_at` among the tool reports actually contributing to that project's severity summary, tracked alongside the existing per-tool aggregation loop. The frontend can now show the true freshness of the severity data distinctly from `last_scan_time` (a follow-up UI change to actually surface it wasn't done here — that's a design decision on how to present it, out of scope for this pass, but the data is now available). Regression test: `tests/test_portfolio_severity_freshness.py` — a project with a fresh Nmap-only rescan (updating `last_scan_time` to now) alongside 30-day-stale Trivy severity data asserts `severity_as_of` is ~30 days older than `last_scan_time`, proving the two are now distinguishable; verified this fails without the fix (removed the field, confirmed `KeyError`-equivalent test failure, then restored it). Full suite: 483 passed. |
| 115 | `get_developer_report` and `get_file_measures` have zero RBAC/project-ownership check — same IDOR family as #35/#36 | `backend/app/api/reports.py:665-779` (`get_developer_report`, only checks the project *exists*, no `_verify_project_ownership`/RBAC call), `:782-791` (`get_file_measures`, zero ownership check of any kind on a raw `component_key` param) | High | **Done** | Every other endpoint in this file (8 of 10) consistently calls `_verify_project_ownership` or an equivalent RBAC check. Any authenticated user can `GET` per-file Sonar issues, coverage/complexity measures, and quality-gate detail for a project they have no assignment to; `get_file_measures` is worse — arbitrary `sonar_key:file_path` combos across the whole system, no project scoping of any kind |
| 116 | `project_groups.py`'s entire cross-project rollup API has no project-scoping at all | `backend/app/api/project_groups.py` (whole file — no `rbac`/`has_project_access` calls anywhere), specifically `get_project_group` (`:83-126`) | Medium | **Done** | Added RBAC scoping across the whole file. `list_project_groups`/`get_project_group` now filter through `rbac.get_effective_project_ids()`: a group is only visible if the caller has access to at least one of its assigned projects (admins see everything, matching the existing `has_project_access` "empty set = all" convention), and `get_project_group` returns 404 — not 403 — for a group entirely outside the caller's scope so its existence isn't leaked. Within a partially-visible group, `assigned_scans` and the aggregated `severity_summary`/`total_findings` are now filtered to only the caller's own accessible projects — added an `allowed_project_ids` param to `ProjectGroupingService.get_group_aggregated_report()` for this. All mutating endpoints (create/update/delete/auto-assign/refresh/bulk-assign/add-assignment/remove-assignment) now require `rbac.can_manage_project()` (admin or team_lead), mirroring the existing project-management gate — a plain developer can view groups they have access to but not modify them. Regression test: `tests/test_project_groups_rbac.py` — proves a dev scoped only to `proj-a` sees just `proj-a` in a mixed group's `assigned_scans` (previously got both), gets 404 on a group made entirely of projects outside their scope, still sees the group in the list because of the partial overlap, and gets 403 attempting to delete a group. Full suite: 466 passed. |
| 117 | `deduplicate_findings`'s hash includes `severity`, so a legitimately re-triaged severity creates a duplicate instead of updating | `backend/app/services/project_grouping.py:66-76` | Low | **Done** | Removed `severity` from `_compute_finding_hash`'s key entirely — identity is now based purely on title/host/cve/package/file/line (plus the #48 tool fallback), so a re-triaged or cross-tool-rescored finding correctly collapses into its existing entry instead of appearing as a second one. This also fixes the specific cross-tool case the hash was originally built for: the same CVE reported by two tools at two different severities (common — tools score CVSS slightly differently) now dedups correctly, which it did not before this fix. Regression test: `tests/test_finding_dedup_hash.py::TestSeverityExcludedFromIdentity` — same CVE at High (Trivy) vs Critical (Dependency-Check) now dedups to 1 result; a re-triaged Medium->High finding at the same file/line also dedups to 1. Full suite: 471 passed. |
| 118 | `cleanup_expired_reports` deletes by absolute age, not by "superseded" — would silently zero out low-scan-frequency projects from every rollup if #16 ever gets it scheduled | `backend/app/tasks/cleanup_tasks.py:11-45` | Low | **Done** | Live risk now that #16 actually got the cleanup task scheduled (Celery beat, done earlier this session) — fixed before it could bite. Added a `NOT EXISTS` correlated-subquery filter to the bulk delete: excludes each `(project_id, tool_name)`'s current latest report (by `max(created_at)`) from deletion even when it's past `expires_at`. A project only loses data once genuinely superseded by a newer scan for that tool, never just for going unscanned past 90 days. While writing coverage for this (also closing #67), found and fixed a real pre-existing bug in the neighboring `set_report_expiration`: `days=None` (documented as "permanent") set `expires_at = None` but then unconditionally called `.isoformat()` on it, crashing with `AttributeError` — caught by the broad `except` and silently reported as `{"error": ...}` instead of the success it actually was. Regression tests: `tests/test_cleanup_tasks.py` — a lone expired report with no rescan survives (the literal #118 scenario); a genuinely superseded older report still gets deleted; two tools on the same project are evaluated independently; reports with no expiry are never touched; `set_report_expiration(days=None)` now correctly returns `success: True` instead of a spurious error. Verified the #118 fix's tests fail without it (reverted the subquery filter, confirmed both the lone-report and independent-tools tests failed exactly as described, then restored the fix). Full suite: 489 passed. |

## Positive findings — Issue Lifecycle & Reporting

| # | What | Location | Why it's good |
|---|------|----------|----------------|
| P58 | `detect_regressions` routes the reopen transition through `transition_status` with a distinguishable `change_type="regression"` | `backend/app/tasks/issue_tasks.py:266-268`; generic `resolved_at` clearing for `{REJECTED, ASSIGNED, OPEN}` in `issue_service.py:305-307` | Confirmed correct by the self-audit — keeps the audit trail meaningfully distinguishable from generic "update" entries and benefits from the same cache-invalidation-on-write path as every other transition |
| P59 | New `reject_rescan` endpoint cleanly reuses `rescan_service.reject()`, mirrors `approve`'s reviewer-attribution fields, and is narrowly scoped | `backend/app/api/issues.py` (`reject_rescan`) | Confirmed by independent self-audit as a clean fix for #56 that doesn't touch unrelated `trigger-verify-scan` semantics |
| P60 | `portfolio/overview`'s tool aggregation correctly avoids double-counting | `backend/app/api/portfolio.py:40-46` (`seen` set takes exactly one `ScanReportDB` row per `(project_id, tool_name)`, most recent by `created_at`) | A project's severity total can't be inflated by old superseded reports for the same tool — the only flaw here is the cross-tool timestamp mismatch (#114), not double-counting |
| P61 | `ScanReportDB` upsert (delete-then-insert per `(scan_id, tool_name)`) is applied uniformly across all fetch paths | `fetcher.py:104-118,145-162,208-232` | Means `get_group_aggregated_report`'s scan_id-based query can never see two report rows for the same tool on the same scan — retries/reprocessing can't double-count within a single scan |
| P62 | `_verify_project_ownership` correctly handles both the API-key/service-account bypass and the admin/RBAC path in one reusable place | `backend/app/api/reports.py:135-151`, applied to 8 of 10 route handlers | Genuinely good, reusable pattern — made the two misses (#115) easy to spot by contrast rather than being uniformly absent |
| P63 | `_compute_recent_trend` explicitly declines to show a trend rather than fabricate one against a missing/zero baseline | `backend/app/api/reports.py:26-64` (requires `TREND_MIN_SCANS=2` within a 30-day window, threads `None` through to hide the frontend indicator) | Good defensive design against a common "misleading trend arrow from one data point" mistake |
| P64 | `RiskCalculator.get_trend`'s ±5-point stable band avoids flagging trivial score jitter as a real trend | `backend/app/services/reporting/risk_calculator.py:34-46` | Sound in isolation, independent of the scoring-formula inversion in #113 |

## Architecture & Design Sweep

| # | Finding | Location | Severity | Status | Notes |
|---|---------|----------|----------|--------|-------|
| 119 | `get_build_status`/`get_queue_item` can crash with `AttributeError` whenever `JENKINS_BASE_URL` contains "jenkins" — and the crash also silently disables the timeout-based recovery backstop for that entire 5-minute cycle | `backend/app/infrastructure/http/client.py:78-89` (`_process_response` returns the raw `requests.Response` object, not parsed JSON, whenever `is_jenkins_request` is true), consumed via `.get(...)` calls in `scan_recovery.py:63-134` | High | **Done** | Downstream of the brittle substring check already logged as #20 — but a much more severe consequence than what #20 originally described (crumb-skipping). `requests.Response` has no `.get()` method, so this raises uncaught inside `poll_jenkins_for_active_scans()`, which runs *before* `recover_stuck_scans()` in the same `try` in `run_recovery_task()` — the crash prevents the independent timeout sweep from running too, for as long as any active scan has a Jenkins build/queue id. Currently masked in dev/test/staging only because none of those envs' `JENKINS_BASE_URL` values happen to contain "jenkins" — will fire the moment a real deployment uses a natural hostname like `jenkins.internal.corp` |
| 120 | `npm run generate:types` has been completely broken since the commit that added it — the script file was never created | `package.json:11` (`python3 scripts/generate-frontend-types.py`); no `scripts/` directory exists anywhere in the repo | High (maintenance trap) | **Done** | Confirmed by actually running it — `ENOENT`. Git history shows the irony directly: a commit deleted the old root-level `generate-frontend-types.py` with the message "replaced by npm run generate:types" but the replacement was never committed. No CI check exists to catch frontend/backend type drift (the CI added for #64 runs tests/lint/typecheck, not a types-diff check), so any backend schema change since that commit could have silently diverged from `src/types.ts` with zero signal the whole time | Restored from git history (a281a32) to `scripts/generate-frontend-types.py`. Also fixed two real bugs found while verifying it: (1) `schema_to_typescript_type` recursed infinitely on every property via eager dict-literal evaluation — the script had never actually run successfully even once, since it was first committed; (2) it referenced `$ref` schema names (e.g. `StageResult`) without ever defining them, which compiles as loose JS but is a hard `tsc` error now that this file exists in a type-checked `src/`. Added a worklist pass that emits every referenced schema. Verified end-to-end against the live backend's OpenAPI schema — output is clean under `tsc -b`. Output file gitignored (never commit a stale snapshot — that's the exact problem this script exists to prevent). |
| 121 | Duplicate, non-synchronized TanStack Query keys for the same "scan history" data | `src/hooks/useScanHistory.ts:12` (`['scans', projectId]`, used by `UnifiedReportPage.tsx`) vs `src/pages/ScanHistoryPage.tsx:21` (`['scan-history', projectId]`, independently defined for the same data) | Medium | **Done** | Confirmed both hooks call the exact same endpoint (`/projects/{id}/scans`, via `api.scans.getHistory` and `api.projects.getScanHistory` — both just proxy to it). Changed `useScanHistory.ts`'s query key from `['scans', projectId]` to `['scan-history', projectId]` to match `ScanHistoryPage.tsx`, so both consumers now share one cache entry. Also updated `useApproveRescan` in `useRescanRequest.ts` (which previously only invalidated `'scans'`) to invalidate both `'scans'` and `'scan-history'`, since approving a rescan starts a new scan and both keys have at least one remaining consumer. Regression test: `src/tests/hooks/useScanHistory.test.tsx` — invalidating `['scan-history']` (what `useScanReset`'s mutations already did) now triggers a refetch of `useScanHistory`'s data; verified this fails without the fix (reverted the key change, confirmed the refetch never happened and the test timed out waiting for a second call, then restored the fix). `tsc -b` clean, lint clean, targeted vitest run passed (a pre-existing, unrelated vitest worker-pool flake intermittently kills random unrelated test files under full-suite load — same known issue noted earlier this session, reproduced on files this change didn't touch). |
| 122 | `JenkinsClient._get_csrf_token()` is dead/inert whenever the "generic" HTTP response path is taken | `backend/app/infrastructure/jenkins/jenkins_client.py:28-46` (`"crumb" in response` checks membership against raw response *bytes*, not a parsed string/dict — never matches) | Low | **Done** | Silently no-ops (`csrf_token` stays `None` forever, no exception). The crumb that's actually used comes from `HttpClient._get_jenkins_crumb` inside `request()`, or — in the realistic dev/test/staging config — from this same method genuinely working against parsed JSON when `is_jenkins_request` happens to be False. Harmless today only by accident of which code path executes; confusing to a future maintainer who trusts the method name and return value | Fixed as a side effect of #119's fix — `_process_response` now always returns parsed JSON for GET requests regardless of `is_jenkins_request`, so `_get_csrf_token`'s `"crumb" in response` check now works correctly against a real dict in every environment, not just by coincidence. |
| 123 | Serial, unbatched Jenkins polling in the recovery sweep compounds #119's blast radius | `backend/app/services/scan_recovery.py:137-194` (`poll_jenkins_for_active_scans`) | Medium | **Done** | Left the sequential single-thread design as-is (batching/async polling is an architecture-level change out of scope for this pass), but fixed the two concrete blast-radius issues: (1) `get_build_status`/`get_queue_item` now pass `timeout=10` instead of the HTTP client's 30s default — these are lightweight status checks, so a hanging response now costs at most ~10s of the sweep instead of ~30s; (2) wrapped the per-scan handler call in the sweep loop in its own try/except (with `db.rollback()` + `continue`) as defense in depth beyond #119's fix inside the handler functions themselves — an exception of any type for one scan (not just the specific ones #119 already handles) can no longer abort the loop before the rest of that cycle's candidate scans are checked. Regression test: `tests/test_poll_jenkins_sweep_isolation.py` — one scan's status check raises `AttributeError` (simulating an unexpected bug/edge case #119's narrower catch wouldn't cover), asserts the healthy scan later in the candidate list still gets checked and updated; verified this fails without the fix (reverted the try/except, confirmed the exception propagated and aborted the sweep before the healthy scan was reached, then restored the fix). Full suite: 480 passed. |
| 124 | `GET /reports/projects/{id}/reports/summary` (no `scan_id`) returned every `ScanReportDB` row ever created for the project, not the latest per tool | `backend/app/api/reports.py:186-232` (`get_reports_summary`) | **High** | **Done** | Found live: rebuilt and redeployed the app from current source to actually test the "dashboard/reporting is unusable" report, and this endpoint's real response for an active project showed the same tool (`sonar`) 6 times with wildly different values (several stale/all-zero), `dependency_check` and `trivy_fs` each duplicated too, and `total_findings: 271` where the real current count was 88 — a 3x+ inflation from summing every historical report instead of just the current one. Root cause: the query had no `scan_id` filter *and* no "latest per (project_id, tool_name)" collapse — the exact class of bug already fixed in `portfolio.py` and `project_grouping.py` earlier this session, just missed here. `ExecutiveSummaryPage.tsx` calls this exact no-`scan_id` path for every project in the portfolio, so this was corrupting the one page built specifically for a leadership-level "current state" rollup — the worst possible page for numbers to be silently wrong on. Fixed by collapsing to one report per tool (latest `created_at`) when no `scan_id` is given; an explicit `scan_id` (used by `ProjectReportsPage`/`UnifiedReportPage`, which were both already unaffected — they always pass one) still returns exactly that historical scan's reports, unchanged. Regression test: `tests/test_reports_summary_dedup.py` — a tool rescanned 3 times now reports only its latest severity counts (13, not 5+8+13=26) with exactly one entry per tool; an explicit `scan_id` still returns only that scan's data. Verified this fails without the fix (reverted, confirmed the inflated/duplicated result, then restored) and verified the live fix against the real duplicated production data (271 → 88, 14 tool entries → 3). Full suite: 506 passed. |
| 125 | Every full page load (refresh, direct URL, new tab) bounced straight to `/login` even with a fully valid session — the access token lives only in React memory and nothing ever hydrated it from the httpOnly cookie on mount | `src/hooks/useAuth.tsx` (`AuthProvider`'s old mount effect just called `setIsLoading(false)`, never `refreshToken()`) | **High** | **Done** | Found live via Playwright: after logging in through the real UI, `page.goto()` to `/dashboard`, `/dashboard/portfolio`, `/dashboard/trends`, any project's `/reports`, etc. — every single one bounced back to `/login`, even though the login had just succeeded seconds earlier. Root cause: `token` is plain in-memory `useState`, seeded only from a legacy `sessionStorage` copy that's honored solely during a 14-day migration grace period (`src/utils/authGracePeriod.ts`) which closed 2026-07-27 — four weeks before this session. Past that date the httpOnly cookie is the *only* credential store, but the mount effect never called the existing (already-built, already-exposed-but-unused) `refreshToken()` / `POST /auth/refresh` to hydrate from it — so `isAuthenticated` (`!!token`) started `false` on every hard navigation regardless of session validity, and `ProtectedRoute` redirected accordingly. This is likely the actual "Dashboard/Reports pages look broken/unusable" report: any bookmarked/shared report link, or a plain page refresh, looked like a dead login wall. Fixed by having the mount effect call `refreshToken()` (skipped only when a legacy token is already in memory) before clearing `isLoading`. Regression test: `src/tests/hooks/useAuth.test.tsx` — mocks `fetch` to prove `isAuthenticated` becomes `true` after mount when `/auth/refresh` succeeds (the httpOnly-cookie case) and stays `false` when it doesn't; verified both fail without the fix (fetch never called) and pass with it. Full frontend suite: 144 passed across 42 files, `tsc -b` clean, lint clean. Compounds with #126 below — this fix alone wasn't sufficient until that one landed too, because the refresh cookie it depends on wasn't actually reaching the browser. |
| 126 | The refresh-token cookie never actually persisted — `max_age=0` deletes a cookie immediately, it does not mean "session-only" — and even fixed, its `Path=/auth` never matched the real mounted route `/api/v1/auth`, so the browser would never have sent it back anyway | `backend/app/api/auth.py` (`set_auth_cookies`, plus the matching `delete_cookie` calls in `logout`) | **High** | **Done** | Found live while verifying #125's fix: hydration still failed after the frontend fix, because `curl -c cookies.txt` against the real login endpoint showed the refresh cookie was never written to the jar at all. `response.set_cookie(..., max_age=0)` is the standard browser signal to expire a cookie *immediately* — not "keep until the browser closes" (that's what omitting `max_age`/`expires` entirely does) — so despite the code comment ("session-only: destroyed when browser closes") and tracker item #1's note that this was a deliberate mitigation, the cookie was actually deleting itself before ever being usable. Second, independent bug in the same code: the cookie was scoped `Path=/auth`, but the router is mounted at `/api/v1/auth` (`main.py`) — `/api/v1/auth/refresh` does not prefix-match `/auth`, so even a persisted cookie would never have been attached to the one request that needs it. Existing cookie tests (`tests/test_auth_cookie.py`) never caught either bug because they manually copied the `Set-Cookie` value into a forwarded `Cookie` header instead of relying on the test client's own cookie jar, which bypasses both `Max-Age` and `Path` scoping entirely. Fixed by dropping `max_age=0` (true session cookie) and correcting `Path` to `/api/v1/auth` in both `set_auth_cookies` and `logout`'s `delete_cookie` calls. Regression tests added to `tests/test_auth_cookie.py`: one asserts no `max-age=0` on the refresh cookie, one drives a fresh `TestClient` through login then an *unmodified* `/auth/refresh` call relying purely on the client's cookie jar (the way a real browser would) — both verified to fail against the pre-fix code (`max-age=0` present; refresh returned 401) and pass after. Full backend suite: 509 passed. Verified live end-to-end via curl against the redeployed containers: login → cookie jar now retains `refresh_token` with no `Max-Age` and `Path=/api/v1/auth` → `/auth/refresh` returns 200 using only the jar's cookies. |
| 127 | `GET /reports/projects/{id}/reports/unified`'s `severity` totals dict omitted the `info` key entirely, unlike every other severity aggregation in the same file | `backend/app/api/reports.py:392` (`get_unified_report`, `total_severity = {"critical": ..., "high": ..., "medium": ..., "low": ...}` — no `"info"`), also the `previous_severity` fallback a few lines below | Medium | **Done** | Found live via the same Playwright screenshot pass as #125/#126, once login/hydration was actually working: `UnifiedReportPage.tsx`'s "Info" stat tile rendered completely blank (not "0") on both real projects' report pages. `report.severity.info` was `undefined` because the backend dict it's built from never had that key — confirmed directly against the running API (`.../reports/unified?scan_id=...` → `severity` had only `critical/high/medium/low`). `get_reports_summary` and `get_report` elsewhere in the same file both correctly include `info`; this one aggregation was just missed. Fixed by adding `"info": 0` to both the running-total dict and the no-previous-scan fallback dict. Regression test: `tests/test_unified_report_info_severity.py` — seeds a report with `info: 5` and asserts the unified endpoint's response echoes it; verified this fails without the fix (`KeyError`-shaped assertion: `info` missing from the response dict) and passes with it. Full backend suite: 509 passed. Verified live: both Meraki and It_tools now show "0" under Info on the redeployed unified report page instead of a blank tile. |
| 128 | Sonar findings' `description`/`recommendation` came back empty for most rules — `/api/rules/search` never requested the `htmlDesc`/`htmlNote` fields it needs | `backend/app/services/reporting/parsers/sonar.py` (`_fetch_sonar_rules`, `params` dict had no `f` key) | **High** | **Done** | Triggered by a partner question ("can a developer actually fix an issue from the report alone, or not") that redirected this session from the assignment workflow to report *data completeness*. Checked live: 151/184 (82%) of real Sonar findings on the Meraki project had `description` empty — not a UI gap, the field itself was blank. Root cause: SonarQube's `/api/rules/search` only returns `htmlDesc`/`htmlNote` (and `debtRemFnType`/`debtRemFnOffset`, `name`, `lang`) when explicitly requested via the `f` query param — omitting it (as this code did) silently returns the bare default field set, and `_build_sonar_finding` reads `rule_data.get("htmlDesc")`/`get("htmlNote")` straight from that response with no error when they're absent. Fixed by adding `"f": "htmlDesc,htmlNote,debtRemFnType,debtRemFnOffset,name,lang"` to the rules-search params. Regression test: `tests/test_sonar_issues.py::test_fetch_sonar_rules_requests_html_desc_field` — mocks the HTTP client and asserts the outgoing request's `f` param includes `htmlDesc`/`htmlNote`; verified this fails without the fix (`f` key absent from sent params) and passes with it. Full backend suite: 512 passed. Not independently re-verifiable against a live SonarQube instance from this sandbox (SonarQube isn't reachable here) — the fix takes effect on each project's *next* scan, so existing rows in the DB stay blank until then; verification here is the unit test proving the correct params are sent, not a live before/after description diff. |
| 129 | `_fetch_github_content` (the code-snippet endpoint's GitHub fetch) corrupted the raw-content hostname for every repo URL in the standard `https://github.com/org/repo.git` form | `backend/app/api/projects.py:305` (old: `git_url.replace(_GITHUB_COM, "raw.githubusercontent.com").replace(".git", "")`) | **High** | **Done** | Found live while verifying #130 below: `GET /projects/{id}/code-snippet` returned 404 "File not found in any accessible source" for a file confirmed (via GitHub's API directly) to exist at that exact path/branch. Root cause: after rewriting the host to `raw.githubusercontent.com`, the following `.replace(".git", "")` doesn't just strip the repo's trailing `.git` suffix — `raw.githubusercontent.com` itself contains the literal substring `.git` (right after "raw"), so the blind replace corrupted the hostname to `rawhubusercontent.com`, which doesn't resolve (`httpx.ConnectError: Name or service not known`), silently caught by the caller's `except Exception: pass` and surfaced as an indistinguishable 404. This affected **100% of real projects** in this deployment (both Meraki's and It_tools' `git_url` end in `.git`). Fixed by building the raw URL from the split repo path instead of a global string replace. Regression test: `tests/test_code_snippet_github_url.py` — asserts the constructed URL is `https://raw.githubusercontent.com/org/repo/...` (not `rawhubusercontent.com`) for both `.git`-suffixed and bare URLs; verified this fails without the fix (reproduces the exact `rawhubusercontent.com` corruption) and passes with it. Full backend suite: 512 passed. Verified live end-to-end via curl against the redeployed backend: the endpoint now returns real file content with a correct `git_url` deep link. |
| 130 | `IssueDetailModal` never called the (fully built, already-deployed) code-snippet endpoint at all — every finding with a file/line showed "No code snippet available" regardless of whether real source was fetchable | `src/components/IssueDetailModal.tsx` (no fetch of `api.issues.getCodeSnippet`, despite that client function and its backend endpoint both existing and working) | Medium | **Done** | Same partner question as #128 — "can a dev fix this from the report" surfaced that the backend (`GET /projects/{id}/code-snippet`, `backend/app/api/projects.py:334`) and even the frontend API client (`api.issues.getCodeSnippet`, `src/services/api.ts:353`) were both fully implemented, but zero call sites existed anywhere in the frontend — confirmed via grep. `CodeSnippet.tsx` already accepted `isLoading`/`error`/`gitUrl` props, clearly built for an async fetch that was simply never wired into the modal that renders it. Fixed by adding a `useQuery` in `IssueDetailModal` (gated on `file_path`/`line_number` present and `code_snippet` not already populated) and passing its result into `CodeSnippet`; a 404 (file genuinely not found — private repo, deleted file, etc.) degrades to the existing "No code snippet available" placeholder rather than showing a scary error box, reserving the error state for real failures. Regression tests added to `src/tests/components/IssueDetailModal.test.tsx`: one asserts `getCodeSnippet` is called with the right args and the returned content/GitHub link render; one asserts a 404 falls through to the placeholder, not an error. Verified the first fails without the fix (`getCodeSnippet` never called, times out). Full frontend suite: 146 passed, `tsc -b`/lint clean. This fix alone was not sufficient live until #129 and #131 also landed — see those for why. |
| 131 | `IssueResponse`'s `file_path`/`line_number`/`tags`/`code_snippet_language`/`rule_name`/`git_url` were plain `@property`, never `@computed_field` — none of them were ever actually in the serialized API response; two list endpoints bypassed the schema entirely | `backend/app/schemas/issue.py` (`IssueResponse`, six properties); `backend/app/api/issues.py` (`get_tool_issues`, `get_my_issues` — neither had a `response_model`); `backend/app/services/issue_service.py` (`get_my_issues`'s hand-rolled `_to_dict` never included these fields at all) | **High** | **Done** | The deepest of the four bugs found chasing the same partner question — this is *why* #130's fix didn't work live even after #129 fixed the GitHub URL: `curl`ing a real issue directly showed `location: {file_path, line}` present but **no top-level `file_path`/`line_number` key at all** in the JSON. In Pydantic v2, a plain `@property` is never included in `.model_dump()`/JSON output — only `@computed_field` properties are — so despite `location` being fully populated in the database for all 151 real Sonar findings on a project, every single one showed "—" in the Triage table's Location column, and the code-snippet fetch (gated on `file_path`/`line_number` being present) never fired for a single real finding. Separately, `GET /issues/projects/{id}/tools/{tool}` and `GET /issues/my` (the two list endpoints actually feeding Triage/My Issues) had no `response_model` at all and returned either raw ORM objects or a hand-rolled dict (`_to_dict`) — neither ever went through `IssueResponse`'s computed fields regardless, an independent instance of the same root problem. Fixed by decorating all six properties with `@computed_field`, adding a `ToolIssuesResponse` schema and a `list[IssueResponse]` `issues` field to `MyIssuesResponse`, wiring `response_model=` onto both routes, and replacing `get_my_issues`'s `_to_dict` with the same raw-ORM-object pattern `get_tool_issues` already used (letting `from_attributes=True` handle serialization consistently in one place instead of two hand-maintained field lists). Regression tests: `tests/test_issue_schemas.py::test_derived_fields_are_actually_serialized` (asserts `.model_dump()` actually contains `file_path`/`line_number`/`tags`/etc.), `tests/test_issue_api.py`'s two new `..._include_file_path_and_line_number` tests (one per list endpoint, end-to-end through the real API). All three verified to fail without the fix (`KeyError: 'file_path'`) and pass with it. Full backend suite: 515 passed. Verified live end-to-end via Playwright: the Triage table's Location column now shows real `file:line` for every row, and opening a finding's detail modal renders real GitHub-sourced code with the correct highlighted line — the full #128–#131 chain working together on a real deployed project. |
| 132 | OWASP Dependency-Check findings had no fix version at all — a developer got a CVE ID and a description, nothing telling them what version actually fixes it | `backend/app/services/reporting/parsers/depcheck.py` (`_parse_vulnerability`/`_build_recommendation` never set `fixed_version`/`fix_command`, unlike `trivy.py` which gets `FixedVersion` for free from Trivy's own scanner output); `backend/app/schemas/issue.py` (`package`/`package_version`/`fixed_version`/`fix_command` weren't exposed on `IssueResponse` either — same `@computed_field` gap as #131); `src/components/IssueDetailModal.tsx` (didn't render them even where `FindingDetailModal.tsx`/`IssueDetailPanel.tsx` already did) | Medium | **Done** | Direct follow-up to the partner's "can a dev actually fix this from the report" question (#128–131) — explicit ask to close the one tool left partial. OWASP's own Dependency-Check report format simply has no fix-version field (confirmed by inspecting real report JSON) — Trivy gets this for free because its scanner emits `FixedVersion` directly; Dependency-Check needs an external lookup. Implemented via OSV.dev (`GET /v1/vulns/{cve}`, no API key needed), deduped per report (same CVE flagged against multiple dependency files only triggers one lookup) and capped at 30 unique CVEs/report to bound worst-case latency since this runs synchronously inside report parsing. Caught a second real bug while live-verifying the first: querying a bare CVE ID sometimes resolves to a record whose only range is `type: GIT` — its `fixed` event is a raw commit hash (confirmed live for CVE-2021-23337: `c6e281b8...`), not a version string; showing that to a developer as "the fix" would be actively misleading. Fixed by only accepting `SEMVER`/`ECOSYSTEM`-typed ranges, falling back to the record's first `GHSA-*` alias (ecosystem-specific, almost always has a clean semver range) when the direct CVE lookup doesn't — confirmed live this correctly resolves to `4.17.21` via the GHSA alias for the same CVE. Also added `package`/`package_version`/`fixed_version`/`fix_command` as real `@computed_field`s on `IssueResponse` (they already existed in `extra_metadata` from a prior fix but were never exposed, the exact bug class #131 fixed for other fields) and rendered them in `IssueDetailModal` matching `FindingDetailModal`'s existing green "→ version" / fix-command styling, so both "developer view" surfaces show the same data instead of one having it and the other not. Regression tests: `tests/test_depcheck_fix_version.py` (fix-version lookup, dedup, graceful failure on network error, non-CVE IDs never looked up, GIT-range rejected + GHSA fallback — this last one verified against real live OSV.dev responses for CVE-2021-23337 before being encoded as a mock-based test) and a new `IssueDetailModal` test asserting the fix-version/fix-command UI renders; both verified to fail without their respective fixes. Full backend suite: 520 passed; frontend: 147 passed, tsc/lint clean. Verified live end-to-end against the real OSV.dev API from the redeployed backend container (not staging DB data — no dependency_check findings exist in this deployment currently) — a synthetic Dependency-Check report for lodash CVE-2021-23337 parses to `fixed_version: "4.17.21"`, `fix_command: "Upgrade lodash-4.17.15.tgz to version 4.17.21 or later"`. |
| 133 | `portfolio.py`'s entire cross-project rollup API had no RBAC scoping at all — the whole file | `backend/app/api/portfolio.py` — `get_portfolio_overview`, `get_project_tool_detail`, `get_portfolio_trends`, `get_team_workload` all took `_current_user=Depends(get_current_user)` (note the leading underscore — literally unused) and never called into `RbacService` | **Critical** | **Done** | Found live during a partner-requested deep dive into "how do the report-actionability fixes help management" — asked me to look for the same class of gap in the dashboards management actually uses. Grepped `portfolio.py` for `rbac`/`has_project_access`/`get_effective_project_ids`: zero hits, in a file with four routes, all mounted with only `Depends(get_current_user)` (`main.py:165,185` — authentication, not authorization). This is the exact bug class already fixed for `project_groups.py` in #116 (same session) — `portfolio.py` never got that pass. Real impact: a developer scoped to a single project could pull `/portfolio/overview`'s severity/risk-score for every project in the system, `/portfolio/project/{id}/tools`'s per-tool breakdown and Sonar metrics for any project by ID, `/portfolio/trends`'s severity *and* coverage trend blended across every project (not just theirs), and `/portfolio/team-workload`'s per-developer issue counts across the whole org. Fixed by adding `RbacService` scoping to all four: admins unchanged, non-admins filtered to `rbac.get_effective_project_ids()` (empty scope returns an empty/zeroed response, not an error), and the single-project tools endpoint returns the same "Project not found" response for both a genuinely missing project and one outside the caller's scope — same non-leaking-existence convention as #116. Also caught and fixed the coverage-trend query specifically not being project-filtered even though the severity query right above it was getting scoped — same root cause, would have reintroduced the leak for one field if missed. Regression tests: `tests/test_portfolio_rbac.py` — 8 tests across all four endpoints, each proving a project-scoped dev sees only their own project's data (admin still sees everything); the four dev-scoped tests verified to fail without the fix (e.g. `assert {'proj-a', 'proj-b'} == {'proj-a'}` — both projects leaked). Full backend suite: 528 passed. Verified live against the redeployed backend: service-account/admin access unchanged (still sees both real projects, correct workload/trends totals) via curl and a full Playwright re-pass of every dashboard page — no regressions. |
| 134 | UX: five nav items (Dashboard, Security Overview, Trends, Team Workload, Executive Summary) largely duplicated each other — Dashboard and Security Overview called the identical `portfolio.getOverview` endpoint and rendered near-identical content; Executive Summary showed the same aggregate numbers through a second, independently-maintained pipeline (`reports.getSummary` looped per-project) that could silently drift from the first | `src/pages/DashboardPage.tsx`, `src/pages/PortfolioDashboardPage.tsx` (deleted), `src/pages/TrendAnalysisPage.tsx` (deleted), `src/pages/TeamWorkloadPage.tsx` (deleted), `src/components/Layout.tsx` | N/A (UX) | **Done** | Partner-directed redesign, not a bug fix — explicit ask to cut the nav down to 2 items and stop the duplication. Merged Security Overview, Trends, and Team Workload into `/dashboard` as three tabs (`?tab=trends`/`?tab=workload`, URL-addressable and deep-linkable) instead of separate pages; folded Security Overview's one non-duplicate piece (Tool Comparison chart) into the Overview tab and dropped everything else from it (pure duplicate of Dashboard's own stat cards/severity donut/quality gate). Legacy URLs (`/dashboard/portfolio`, `/dashboard/trends`, `/dashboard/workload`) redirect to the new tabs so old bookmarks/links keep working. Executive Summary intentionally kept as its own page (genuinely different audience/purpose — a leadership snapshot with risk score, top risks, and an all-projects table) but not yet repointed off its separate aggregation pipeline (still a real drift risk, just not addressed in this pass). Sidebar nav dropped from 5 items to 2 (Dashboard, Executive Summary). Test suites for the three merged pages consolidated into `DashboardTrendsTab`/`DashboardWorkloadTab`/`DashboardPage` tests (tab-switching, deep-link, and all prior regression coverage preserved: #106, #107, #60). Full frontend suite: 152 passed, tsc/lint clean. Verified live via Playwright: all three tabs render correctly, all three legacy URLs redirect to the right tab. |
| 135 | Team Workload's developer cards and unassigned-severity tiles were dead ends — no way to see the underlying issues, and clicking through (once wired) landed on a filtered view that silently showed zero results due to two independent bugs it exposed | `src/pages/TeamWorkloadPage.tsx` → `src/components/dashboard/DashboardWorkloadTab.tsx` (click-through wiring); `src/pages/IssuesTriagePage.tsx` (severity case-sensitivity + pagination cap); `backend/app/schemas/issue.py` (`IssueCreate`/`IssueUpdate` severity normalization) | High | **Done** | Flagged in an earlier deep dive as the #1 dev-facing gap ("Team Workload is a dead end"). Wired developer cards and unassigned-severity tiles to real links into `/issues?assignee=...&status=...&severity=...` (Triage page now reads these via `useSearchParams` as initial filter state). Live-verified the click-through and found it landed on "0 issues" despite Team Workload showing 12 unassigned criticals — traced to two separate, independent, pre-existing bugs the new link exposed: (1) the Triage page's severity filter compared `severityFilter.includes(i.severity)` case-sensitively, and the DB has genuinely mixed-case severity (`SELECT severity, count(*) ... GROUP BY severity` showed one stray `"Critical"` row alongside 26 `"critical"` — the scan-ingestion path lowercases severity, but nothing enforced it at the schema boundary, so a direct `POST /issues` or any future integration could still insert mixed case); (2) the Triage page's per-tool fetch loop had a hard `page <= 4` cap (100 issues), silently dropping the rest of a 151-issue Sonar tool — most of the real unassigned criticals were past page 4 and never fetched at all. Fixed both: added a `field_validator` lowercasing `severity` on `IssueCreate`/`IssueUpdate` (prevents new mixed-case rows), made the frontend filter/sort case-insensitive as defense-in-depth for existing data, and removed the artificial page cap (now fetches until a page comes back short, with a generous 200-page sanity ceiling against a runaway loop). Regression tests: `tests/test_issue_schemas.py::test_severity_is_lowercased`, three new `IssuesTriagePage.test.tsx` tests (case-insensitive matching, full-pagination fetch, assignee deep-link) and two new `DashboardWorkloadTab.test.tsx` tests (link hrefs); all verified to fail without their respective fixes. Full backend suite: 529 passed; frontend: 155 passed, tsc/lint clean. Verified live end-to-end: clicking a Team Workload tile now correctly lands on the matching filtered issue list instead of a false "no issues match your filters." |
| 136 | The code-snippet feature (fixed in #129/#130/#131) still didn't work for any project with a private GitHub repo — GitHub raw fetch can never succeed for one regardless of the URL-corruption fix, and there was no fallback at all | `backend/app/api/projects.py` (`get_code_snippet`); `backend/app/services/reporting/parsers/sonar.py` (`fetch_sonar_source`) | High | **Done** | Partner-reported live: Meraki's findings still showed no code snippet even after #129–131 landed. Confirmed via GitHub's own API (`GET /repos/meraki-pda/api` → 404) that the repo genuinely isn't publicly reachable — no fix to the URL construction can work around that. SonarQube already indexed the source when it scanned the project, so it works regardless of repo visibility — and `fetch_sonar_source(component_key)` was already implemented in `sonar.py` (correct API call — its response-parsing turned out to be wrong, see #137) but had zero call sites anywhere in the codebase, the identical "built but never wired" pattern already found once for this same endpoint (#130). Wired it in as a third fallback (GitHub → local workspace → SonarQube), extended `fetch_sonar_source` to accept optional `from`/`to` line-range params so it only requests the needed window instead of a whole file, and added `_strip_sonar_source_markup` — SonarQube's `/api/sources/show` returns each line's code wrapped in syntax-highlighting `<span>` tags, not plain text, which would have rendered as literal markup in the frontend's own syntax highlighter if fed through unstripped. `git_url` is `null` for the SonarQube-sourced case (no reliably correct SonarQube file-view URL format to link to across versions — left unset rather than guess and ship a broken link). Regression tests: `tests/test_sonar_issues.py` (from/to params sent correctly, empty result without a token), `tests/test_code_snippet_sonar_fallback.py` (full endpoint fallback chain end-to-end, markup-stripping unit tests); the endpoint-level test verified to fail without the fix (404 instead of 200). Full backend suite: 536 passed, tsc/lint clean (no frontend changes). Verified live against the redeployed backend: confirmed the fallback fires for Meraki (backend log: `Error fetching SonarQube source: All connection attempts failed` — expected, since this sandbox has no real SonarQube instance reachable — degrades to a clean 404 rather than crashing; the actual staging deployment's real SonarQube instance would resolve this successfully). |
| 137 | `fetch_sonar_source`'s response parsing was wrong — the "fully implemented" function from #136 had never actually been run against a real SonarQube instance, and its assumed response shape didn't match reality | `backend/app/services/reporting/parsers/sonar.py` (`fetch_sonar_source`) | High | **Done** | Caught immediately after #136 shipped, once the partner had me start the (previously stopped) `docker-sonarqube-1` container and a real instance became reachable for the first time this session. Calling the newly-wired fallback against real data for Meraki still returned 404 — traced it live: `fetch_sonar_source` raised `'list' object has no attribute 'get'` internally (silently swallowed by its own broad except-block) because the real `/api/sources/show` response is `{"sources": [[line, code], [line, code], ...]}` — each entry a `[line, code]` array pair — not `{"sources": [{"line": ..., "code": ...}, ...]}` as the code assumed. Confirmed directly against the live SonarQube 26.5.0 instance (`curl .../api/sources/show?key=Meraki:...` → literal `{"sources":[[1,""]]}`). This is the second bug found in this one function in as many hours — the first pass (#136) genuinely couldn't have caught this, since nothing in this codebase or its test suite had ever executed this function against a real server before now; every existing "test" was mocking the (wrong) assumed shape back at itself. Fixed the parsing to index into the pair (`s[0]`/`s[1]`) instead of dict-`.get()`, with a defensive `isinstance`/length check per entry. Updated both existing regression tests (`test_sonar_issues.py`, `test_code_snippet_sonar_fallback.py`) to mock the *real* confirmed shape instead of the wrong one — they were previously "passing" only because they tested the code's incorrect assumption against itself. Full backend suite: 536 passed. Verified live end-to-end against the real, now-running SonarQube instance and a real Meraki file (`app/api/auth_controller.py`): the code-snippet endpoint returns actual source with correct line numbers and `source: "sonar"`. This is the first fix in the #128-#137 sequence verified against a genuinely live SonarQube instance rather than a mock or a graceful-failure path — worth noting as a caution: anything else touching the Sonar integration this session that was only mock-verified carries the same unverified-against-reality risk this one turned out to have. |
| 138 | UX: `ProjectReportsPage` required a manual tool click before any findings showed at all, its 4-card sidebar (Scan Info/Severity/Tools/Actions) could outgrow the viewport and push content below the fold, and every occurrence of a repeated finding rendered as an identical, indistinguishable placeholder row | `src/pages/ProjectReportsPage.tsx`, `src/components/reports/ProjectReportLayout.tsx`, `src/components/reports/FindingsTable.tsx`, `src/components/FindingDetailModal.tsx`, `src/types.ts` (`Finding`) | N/A (UX) | **Done** | Partner-directed redesign after reviewing the live page: "too much scrolling," "not dynamic," "have to specifically select the tools." Confirmed via code read that `selectedTool` started `null` and gated the entire findings view behind a "Select a Tool" empty state, and `ProjectReportLayout` was a fixed-height two-column layout with a 4-card sidebar (Scan Info/Severity/Tools/Actions) that visibly overflowed on a real project with 8 pipeline stages. Redesigned per partner-approved options: (1) findings now render immediately for all tools combined, no click required; (2) the sidebar collapsed into one compact header row (scan meta + severity pills + action buttons), tools moved into the findings list itself as filter chips; (3) a finding with multiple occurrences now shows one summary row with an "N occurrences" badge, collapsed by default, expanding on click to show each occurrence individually. While building the expand behavior, added real file:line display (`locationOf()` in `FindingsTable.tsx`, plus a File field in `FindingDetailModal`) — the raw-findings `Finding` type never even declared `file_path`/`line_number` despite the API already returning them for real (non-stale) data, so this system showed the same "Meraki" host placeholder on every row regardless of whether real location data existed, mirroring the `location`-never-serialized bug already fixed for the Issues system in #131. Also caught and fixed a real, independent grouping bug surfaced by writing tests for the new expand behavior: findings were grouped by `rule \|\| description \|\| 'unknown'`, so any two *unrelated* findings that both lacked a `rule` (common — many of Meraki's real findings have none) silently merged into one group under whichever title arrived first; fixed by keying on `rule` when present, else `title` (never a shared literal `'unknown'`). Regression tests: `FindingsTable.test.tsx` (5 tests — all-tools-by-default, tool-chip filtering, collapsed/expanded occurrence rows, "Location unavailable" graceful fallback, single-occurrence direct-open) and `ProjectReportLayout.test.tsx` (compact header, no separate Tools panel); the grouping-key test caught the real bug live during development (failed before the key fix, passed after). Full frontend suite: 161 passed, tsc/lint clean. Verified live via Playwright against Meraki's real 88-finding scan: all findings visible on load, tool chips filter correctly (79/8/1 split), CVE and rule-based findings each get their own distinct row, expand/collapse works, compact header fits without scrolling. |
| 139 | `UnifiedReportPage`'s findings table had the exact same flat-row problem #138 fixed on `ProjectReportsPage` — worse, since it had no grouping at all, not even the old rule-based grouping `ProjectReportsPage` had before today | `src/pages/UnifiedReportPage.tsx` | High | **Done** | Follow-up from a `/research` pass launched to decide what to work on next after #138 — asked a background agent to check whether `UnifiedReportPage` and `DeveloperReportPage` shared the same problems just fixed, grounded in current source rather than assumption. Confirmed: every finding occurrence rendered as its own full `<table>` row with zero grouping, and used `finding.host \|\| finding.package \|\| '-'` for location — same missing `file_path`/`line_number` gap as #138, never fixed here independently. The page already used the same `FindingDetailModal` as `ProjectReportsPage`, so the fix was straightforward reuse: replaced the page's own `<FilterBar>` + raw `<table>` + local `search`/`selectedSeverities`/`selectedTools`/`filteredFindings` state with the `FindingsTable` component built for #138 (which has its own search/severity/tool filtering and the grouped/collapsible occurrence display), fed from the same `toolSummariesArray` already computed on this page for `ToolBarChart`. Regression test added: `UnifiedReportPage.test.tsx` — two findings sharing a rule now collapse into one row with a "2 occurrences" badge instead of two separate rows. Full frontend suite: 162 passed, tsc/lint clean. Verified live via Playwright against Meraki's real 88-finding unified report: findings grouped into Security Vulnerabilities (20) / Reliability Issues (68) sections, tool filter chips work, occurrence counts correct, no duplicate flat rows. `DeveloperReportPage`'s forced two-click funnel (click a file, then click an issue within it) and the three-way `IssueDetailPanel`/`IssueDetailModal`/`FindingDetailModal` sprawl remain open — not addressed in this pass. |
| 140 | `DeveloperReportPage` required two clicks (pick a file, then pick an issue within it) before any real issue detail showed — same "content should show immediately" gap as #138/#139, just not yet applied here | `src/pages/DeveloperReportPage.tsx` | Medium | **Done** | Second item from the `/research` follow-up list. Confirmed via code read: `selectedFile`/`selectedIssue` both started `null`, and the file-health list, issue list, and `IssueDetailPanel` render top-to-bottom conditionally on each selection existing — a developer landing on this page saw only a bare file list until clicking twice. The file-health browsing UI itself (per-file coverage/complexity/duplication metrics via `FileHealthCard`) has genuine standalone value and isn't the flat-duplicate-row problem #138/#139 were — issue rows here already show distinct `Line N`/rule/effort per row — so the fix wasn't a restructure, just removing the forced clicks: two `useEffect`s auto-select the file with the worst (highest-severity-weighted) findings on load, then that file's worst issue, so real detail is visible with zero clicks; a manual click on a different file still re-selects that file's own worst issue automatically rather than requiring a second click every time. This page's sidebar-overflow problem was already fixed for free earlier, since it reuses `ProjectReportLayout` (#138). Regression tests: `DeveloperReportPage.test.tsx` — asserts the file with a Critical issue (not the first file in the list, which only has a Low) is auto-selected with its detail already visible, and that clicking a different file re-selects that file's own worst issue; both verified to fail without the fix (`waitFor` timeout — the auto-selected content never appeared). Full frontend suite: 164 passed, tsc/lint clean. Verified live via Playwright against It_tools' real 163-issue developer report: `date-time-converter.models.ts` (29 issues, the worst file) auto-highlights on load with its worst issue's detail already visible, no clicks needed. The three-way `IssueDetailPanel`/`IssueDetailModal`/`FindingDetailModal` modal-sprawl item from the research list remains open — not addressed in this pass. |

## Positive findings — Architecture Sweep

| # | What | Location | Why it's good |
|---|------|----------|----------------|
| P65 | `HttpClient`'s retry config deliberately excludes `POST` from automatic retries, with an explanatory comment | `backend/app/infrastructure/http/client.py:12-30` | Correctly scoped safety choice — avoids double-triggering a Jenkins build via an automatic retry on a request that already reached the server |
| P66 | `cleanup_expired_reports` uses a single bulk `DELETE` with `synchronize_session=False`, not a load-everything-into-memory loop | `backend/app/tasks/cleanup_tasks.py:11-45` | Good practice for what will eventually be a large daily sweep, once #16 gets it actually scheduled |
| P67 | `useFocusTrap.ts` is a complete, correct WCAG-compliant focus-trap implementation | `src/hooks/useFocusTrap.ts` | Tab/Shift+Tab cycling, Escape-to-close, auto-focus on mount, proper listener cleanup — no gaps found |
| P68 | `useDebounce.ts` is a minimal, correctly-implemented generic debounce hook | `src/hooks/useDebounce.ts` | Proper cleanup of the timeout on dependency change/unmount |

---

*Add new rows as we talk. Don't reorder existing IDs — append. Update Status in place when we decide.*
